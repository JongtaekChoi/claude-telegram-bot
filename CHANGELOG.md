# Changelog

## [0.5.0] - Unreleased
### Added
- **조용한 인증 실패를 봇이 먼저 말한다.** Claude Code 는 자격증명을 macOS 키체인과
  `~/.claude/.credentials.json` 양쪽에 둘 수 있는데 **읽는 쪽이 실행 컨텍스트마다 다르다** —
  launchd 로 뜬 봇은 키체인을, 터미널은 (키체인에 접근 못 해) 파일을 읽는다. 터미널에서
  로그인하면 파일만 새로 써지고, 로그인이 리프레시 토큰을 회전시키므로 **키체인 사본은
  갱신도 못 한다.** 봇만 죽고 터미널 테스트는 전부 통과한다.
  (2026-09-02 에 봇 넷이 이 상태로 조용히 실패했고, 원인을 찾는 데 반나절이 걸렸다.)
  - 부팅 시 · 이후 6시간마다 점검해 **오너 DM** 으로 알린다. 만료는 봇이 떠 있는 동안
    일어나므로 부팅 검사만으로는 못 잡는다.
  - 아직 유효해도 **두 벌의 토큰이 다르면** 알린다 — 다음 회전에서 깨질 게 확정이라서.
  - 같은 상태로 도배하지 않는다. 상태가 바뀔 때만 보낸다.
  - 실봇 확인 완료 — 갈라진 상태에서 재시작하니 부팅에서 잡아 오너 DM 이 도착했고, 키체인
    사본을 지운 뒤 launchd 에서 `claude -p` 가 정상 통과하는 것까지 확인했다.
  - Codex 전용 설정에서는 확인하지 않는다. 단 기본이 codex 라도 claude 를 쓰는 방이 하나라도
    있으면 확인한다 — 그 방이 죽는 건 똑같다.
### Changed
- **`/cron add` 가 친 방을 적는다.** 지금까지 채팅에서 추가한 예약 작업에는 `chat` 이 없어서,
  DM 에서 추가해도 결과가 **허용된 방 전부로** 뿌려지고 역할도 `personas[0]` 로 돌았다.
  `/cron add` 는 어느 방에서 쳤는지 알고 있으니 모르는 척할 이유가 없다. 일부러 여러 방에
  보내고 싶으면 config 의 `schedule` 에서 `chat` 을 비우면 된다 — 그 경로는 그대로다.
  `/cron` 목록도 작업마다 **목적지를 같이** 보여준다. 안 보이면 "왜 저 방에도 갔지"가 반복된다.
- **`npm test` 가 동작을 검사한다.** 지금까지 `node --check` 뿐이라 **문법 오류만 잡고 동작은
  하나도 안 봤다.** 하네스는 세션 스크래치패드에만 있었고, 2026-09-02 에 세션이 끊기면서
  다섯 스위트가 한꺼번에 증발했다. `tests/` 로 들여 레포가 갖게 한다.
  - 5스위트 107건 — 자격증명(27) · 마크다운 변환(22) · `schedule[].chat`(25) · 건너뛴 알림(20) · codex 임시 파일 회수(13).
  - `bot.mjs` 는 export 가 없으므로 **소스에서 블록을 떼어** 스텁 위에서 돌린다. 복사본을 두면
    코드가 바뀌어도 테스트는 옛것을 통과시킨다.
  - 앵커가 밀리면 `cut()` 이 **이름을 대고** 실패한다. 예전 하네스는 엉뚱한 `SyntaxError` 로
    죽어서 무엇이 밀렸는지 알 수 없었다. 스위트마다 프로세스가 따로라 하나가 깨져도 나머지가 보인다.
  - `files` 가 허용목록이라 `tests/` 는 npm 배포본에 안 들어간다.
- **죽은 봇이 남긴 codex 임시 파일을 부팅 때 회수한다.** `codex exec` 의 `-o` 출력은 PID 로
  이름을 붙여 `.claude-bot/` 에 남는데 지우는 자리가 없어 **재시작할 때마다 한 개씩 영구히 쌓였다** —
  2026-09-04 에 7월 10일치부터 15개가 있었다. 살아 있는 PID 것은 쓰는 중일 수 있으니 건드리지 않고,
  죽은 것(ESRCH)만 지운다. `EPERM`(다른 사용자가 띄운 봇)도 살아 있는 것으로 본다.
  나머지 찌꺼기(`attachments/` · `*.bak-*` · `session.json`)는 백로그에 있다.
- **실패 문구가 어느 CLI 인지 이름을 댄다.** 예전엔 `Execution error (exit 1)` 로만 감싸서
  Claude 가 실패한 건지 Codex 가 실패한 건지 알 수 없었다 — 실제로 Claude 인증 오류를 보고
  Codex 를 먼저 뒤지는 일이 있었다. 이제 `Claude 실행 실패 (exit N)` 로 나가고, **인증 실패는
  따로 분류해** 원인(자격증명 갈라짐)과 고치는 명령까지 답장에 적는다. 이 오류는 방 하나가
  아니라 **모든 방이 죽는** 종류라 원문만 던지면 아무도 못 읽는다.
- 오류 문구가 영/한 양쪽을 따른다. `classifyClaudeError` 가 한국어를 하드코딩하고 있어서
  영어 사용자에게도 한국어 오류가 나갔다.

- **포럼 상위 토픽은 실행하지 않고 어디로 보낼지 묻는다.** 포럼 그룹은 열면 기본으로 상위에 서고,
  알림에서 들어가도, "모두" 뷰에서 읽다 답장해도 그렇다. 그래서 **특정 토픽에 갈 말이 상위 방으로
  나가는 사고**가 반복됐다. 상위 방도 세션을 가진 실행 방이라 조용히 돌아버리고, 알아차렸을 땐
  엉뚱한 세션이 오염됐고 · 토큰을 버렸고 · 정작 그 말이 가야 할 방은 못 들었다. `/stop` 은 타이핑이
  실행보다 느려서 대개 늦는다.
  이제 상위 토픽에서는 provider 를 띄우지 않고 버튼으로 묻는다 — 형제 토픽마다 하나씩, 그리고
  `▶ 여기서 실행` 과 `❌ 안 보냄`. **누르기 전에는 아무것도 돌지 않으므로, 실수가 "엉뚱한 세션에
  보낸 것"이 아니라 "안 보낸 것"이 된다.** 실패가 시끄러워진다.
  **켜는 명령도 설정도 없다.** 봇이 이미 아는 사실 셋 — 포럼 그룹인가 · 방 키가 그룹 ID 와 같은가
  (= 상위 토픽) · 봇이 아는 형제 토픽이 있는가 — 가 사고가 나는 방과 정확히 일치한다. DM·일반
  그룹·단일 방 봇·토픽을 안 쓰는 포럼은 구조적으로 안 걸린다. 사람이 켜는 걸 잊으면 기능이 있어도
  사고가 나므로 자동이 맞다. 오탐의 대가는 탭 한 번이고, 놓쳤을 때의 대가는 위의 셋이다.
  넘어간 말에는 **머리말을 붙이지 않는다.** `/tell` 은 "옆방에서 알아낸 걸 넘기는" 것이지만 이건
  "처음부터 여기로 왔어야 할 말"이라, 받는 토픽은 사람이 거기서 친 것과 완전히 같게 받아야 한다.
  원본 메시지를 그대로 들고 방 키만 바꾸므로 **첨부·캡션·미디어 그룹·발신자가 다 따라간다** —
  로그나 스크린샷을 붙여 보내다 방을 틀리는 경우가 오히려 흔해서, 첨부를 흘리면 절반만 쓸모 있다.
  묻는 자리는 병합 창 **뒤**라, 붙여넣어 여덟 조각으로 쪼개져 온 로그에도 버튼은 한 번만 뜬다.
  명령(`/status`·`/stop`·`/tell` …)은 그 앞에서 처리돼 상위 토픽에서도 그대로 즉시 동작한다.
  뮤트된 토픽으로는 보내지 않고(`/tell` 과 같은 규칙), 대기는 메모리에만 둬서 재시작하면 만료된다.
  **대화가 이어지는 중이면 묻지 않는다** — 이 방에서 마지막으로 실행한 지 10분 안이면 그냥 돈다.
  상위 방에서 일부러 일을 시키는 사람에게 한 마디마다 탭이 붙는 걸 덜기 위해서다. 끄는 스위치는
  성가심과 보호를 같이 버리지만, 이 창은 보호를 유지한 채 성가심만 덜어낸다 — 새어나오는 사고는
  "다른 토픽에서 한참 일하다 상위에 흘린 첫 메시지"라 정의상 조용한 뒤에 오기 때문이다.
  내용을 보고 방을 추측하는 것이 아니라 시각만 본다.
  설계: [docs/design/room-router.md](docs/design/room-router.md)
- **건너뛴 예약 작업이 건너뛰었다고 말한다.** 지금까지 예약 작업이 건너뛰어지면 `console.warn`
  한 줄이 로그에 남고 끝이었다 — **로그를 읽는 사람이 없으니 회차가 통째로 사라져도 아무도
  몰랐다.** 실제로 로컬 `ctb` 세션이 2시간 반 떠 있는 동안 예약 두 개가 유실됐고, 로그를
  파보고서야 알았다. 안 온 메시지는 눈에 안 띄지만 온 메시지는 띈다.
  - 결과가 갔을 자리(`chat` 이 있으면 그 방, 없으면 예전처럼 전부)에 한 줄 보낸다. 뮤트된 방은
    결과와 똑같이 건너뛴다 — 뮤트는 "여기서 아무것도 하지 마라"다.
  - 원인을 구분한다: **로컬 세션**이면 pid·경과 분·어느 방인지와 함께 `/local` 과 같은
    **종료 버튼**을 붙이고, **앞선 예약 작업**이면 다음 예정 시각에 다시 돈다고 알린다.
  - 알림은 **작업마다 한 시간에 한 번**이다. 5분마다 도는 작업이 오래 막혀도 방을 도배하지 않는다.
  - 실봇 확인 완료 — 락을 잡은 채 매분 작업을 돌렸더니 첫 회차만 알리고 이후 10분은 조용했고,
    락이 풀리자 바로 실행됐다. 종료 버튼은 프로세스 그룹까지 정리했다(잔존 프로세스 0).

- **오너 DM 에서 방을 추가한다 (`/allow`).** 방을 하나 늘리려고 `config.json` 을 고치고 재시작하는
  일 — 즉 **폰에서는 못 하는 일** — 을 없앤다. 새 그룹에 초대하면 봇이 이미 `chatId` 를 알려주는데,
  정작 넣는 건 SSH 로 해야 했다. 그 반 발짝을 잇는다.
  - `/allow` 목록에 출처를 같이 보여준다 — `[config]`(파일에 적힌 것) · `[added]`(`/allow` 로 넣은
    것) · `[adopted]`(그룹 승격으로 물려받은 것).
  - `/allow <chatId>` 는 `state.allowedChatIds` 에 적고 화이트리스트에 **즉시** 밀어 넣는다.
    재시작이 필요 없고 재시작해도 유지된다.
  - `/allow rm <chatId>` 는 state 출신만 지운다. `[config]` 출신이면 "파일에서 지우고 `/restart`"
    라고 정직하게 안내한다 — 지웠다고 말해 놓고 재시작하면 되살아나는 게 제일 나쁘다.
  **봇은 `config.json` 을 쓰지 않는다.** 부팅 `JSON.parse` 에 보호가 없어서 config 가 깨지면
  크래시 루프에 빠지고, 원격 관리하려고 만든 기능이 실패하는 순간 SSH 를 유일한 복구 수단으로
  만든다. 그래서 추가분만 state 에 남긴다(`state.json` 은 파싱 실패 시 `{}` 로 폴백한다).
  **게이트는 방이 아니라 사람(`msg.from.id`)이다.** 그룹 멤버십이 권한이 되면 그룹 관리자가
  사람을 추가하는 순간 어드민이 늘어난다. 오너가 아니면 거절 문구도 보내지 않고 **명령의 존재
  자체를 알리지 않는다** — 에이전트에게 넘기지도 않는다. 넘기면 "권한을 달라"가 그대로 프롬프트가
  되고 `bypassPermissions` 에서는 실제로 config 를 고칠 수 있다. 같은 이유로 `ctb send` 와 `/tell`
  경로도 이 명령에 닿지 못한다(둘 다 `from` 이 없고 본문에 접두사가 붙는다).
  ⚠️ `/allow` 로 연 방은 `config.json` 에 적은 방과 **권한이 완전히 같다.** 그룹이면 그 안의
  전원이다.
  설계: [docs/design/owner-admin.md](docs/design/owner-admin.md)
- **역할이 자기 작업 폴더를 가진다 (`personas[].dir`).** 적으면 그 방의 실행이 그 폴더에서 돈다
  (`projectDir` 기준 상대경로, 절대경로도 받는다). **봇 하나가 프로젝트 여러 개를 맡는 방법**이다 —
  지금 `clawd` 와 `clawd/projects/household-ledger` 봇이 나뉜 이유가 **오직 폴더가 달라서**였다.
  가장 큰 이득은 그 폴더의 `CLAUDE.md` 가 따라오는 것. 역할이 *config 의 프롬프트 + 그 폴더의
  프로젝트 규칙* 으로 합성된다.
  작업 폴더에 딸린 것이 전부 방을 따라간다 — 자식 프로세스의 `cwd`(claude·codex·ollama·커스텀
  명령)와 codex 의 `-C`, `/sessions` 의 폴더 필터, `.ctb-outbox` 와 `.ctb-jobs`, 에이전트에게
  주는 안내 문구에 박힌 경로, `/status`·`/help` 의 작업 폴더 표시, 그리고 터미널의 `ctb --chat`.
  **`/sessions` 필터가 제일 조용히 깨지는 자리다** — 전역 `projectDir` 로 거르면 `dir` 을 가진
  방에서 자기 세션은 하나도 안 보이고 남의 것만 보인다.
  `.ctb-outbox`·`.ctb-jobs` 는 **작업 폴더마다** 생긴다. 작업 감시와 `/jobs` 는 폴더를 전부 훑고,
  폴더가 둘 이상이면 목록에 어느 프로젝트 작업인지 표시한다. 한 폴더만 보면 다른 프로젝트에서
  띄운 작업이 끝나도 아무도 안 알린다.
  방을 지목한 예약 작업(`schedule[].chat`)도 그 방의 폴더에서 돈다.
  `dir` 이 없는 폴더를 가리키면 **그 방만 멈춘다** — 폴더를 만들어 주지도, 다른 역할의 폴더로
  물러서지도 않는다. 둘 다 엉뚱한 프로젝트를 조용히 고치는 길이다. 부팅 로그에 한 줄 남고, 그
  방에서 말을 걸면 왜 안 도는지 알려준다.
  권한(`permissionMode`·`codexSandbox`)은 아직 전역이다. 역할별 권한은 그것 때문에 생기는
  "권한이 안 지켜지는 경로 셋"을 같은 커밋에서 막아야 해서 별도 작업으로 뺐다(6b).
  `dir` 을 안 쓰면 **동작이 예전과 완전히 같다.**
  설계: [docs/design/room-personas.md](docs/design/room-personas.md)
- **예약 작업이 방을 지목할 수 있다 (`schedule[].chat`).** 적으면 그 작업이 **그 방의 역할로**
  돌고(그 역할의 `/remember` 규칙·provider·model까지) 결과도 **그 방에만** 간다. 방 키 하나 또는
  배열을 받는다.
  한 봇이 역할을 하나만 맡을 때는 없어도 됐다. **역할을 여럿 두는 순간 필요해진다** — 예약 작업은
  방 밖에서 돌아서 `personas[0]` 으로 실행되고 결과는 `allowedChatId` 전부에 뿌려진다. 기획
  리포트를 개발자가 쓰고 그게 개발 그룹에도 붙는다. 봇이 갈려 있을 때는 config 가 갈려서 안 보이던
  문제가, 합치는 순간 드러난다.
  `chat` 을 적어도 **메시지를 `handle()` 에 넣지는 않는다.** 그러면 그 방의 plan 고정(승인 대기에
  걸려 영영 실행 안 됨)·뮤트·대기열·세션을 전부 물게 된다. 역할과 주소지만 방에서 빌리고, 실행은
  지금처럼 세션 없이 예약 슬롯에서 한다.
  대상 방은 **시작할 때** `allowedChatId` 로 검증하고 틀리면 그 작업을 통째로 버린다(로그에 에러).
  `send()` 에는 화이트리스트 검사가 없는데 예약은 사람이 안 보는 사이에 돌기 때문에, 오타 하나로
  엉뚱한 방에 나가면 오래 안 들킨다. 뮤트된 방은 건너뛴다.
  `chat` 이 없으면 예전 그대로 — 역할 없이 돌고 전체에 뿌린다.
  설계: [docs/design/room-personas.md](docs/design/room-personas.md)

## [0.4.13] - 2026-08-27
### Added
- **터미널에서 돌고 있는 봇에 일을 시킨다 (`ctb send`).** 지금까지 `ctb` 는 **자기가** `claude` 를
  띄웠다 — 봇은 그 일을 모르고, 방에는 typing 도 안 뜨고, 답도 터미널에만 남았다. `ctb send` 는
  반대로 프롬프트를 **이미 돌고 있는 봇에** 넘기고, 봇이 그 방에서 처리한다. 밖에서 보면 사람이
  텔레그램에 직접 친 것과 구별되지 않는다 — 그 방의 세션·provider·model·plan 고정을 그대로 쓰고,
  방이 바쁘면 그 방 대기열에 쌓이고, 답이 그 방에 남는다. 답은 stdout 으로도 와서 스크립트가 이어
  쓸 수 있다.
  ```sh
  ctb send --chat 플랜 "0.5.0 릴리스 노트 초안 잡아줘"
  ```
  방은 키 그대로도, **이름의 일부**로도 지목한다. 방을 안 주면 아는 방이 하나뿐일 때만 그걸 쓰고,
  여럿이면 목록을 보여주고 멈춘다 — 스크립트가 엉뚱한 방으로 가는 것보다 낫다.
  **기본은 대상 방의 승인(✅/❌)을 받는다.** 부르는 쪽이 에이전트일 수 있는데, 프로세스는 사람이 친
  `ctb` 와 모델이 부른 `ctb` 를 구분하지 못한다 — 구분이 불가능하면 안전한 쪽을 기본으로 둔다.
  `--now` 로 건너뛸 수 있고, 그때도 방에는 "터미널에서 온 요청"이라고 뜬다. 막을 수 없으면 최소한
  보이게 한다. 승인 대기는 10분 뒤 만료되어 터미널이 영영 매달리지 않는다.
  **자기 `ctb` 세션이 쥔 방으로는 못 보낸다** — 봇이 그 방을 미루므로 세션이 끝날 때까지 큐에서 안
  나온다. 조용히 멈추는 대신 그 자리에서 거절한다. 이렇게 밀어 넣은 메시지는 `/tell` 의 홉 1
  규칙을 그대로 받아 **다른 방으로 다시 전달되지 않는다.**
  봇이 안 떠 있으면 예전 방식(로컬 실행)으로 조용히 물러서지 않고 실패한다 — 물러서면 방 안내도
  typing 도 없이 돌아서, 원한 것과 정반대인데 성공한 것처럼 보인다.
  **터미널 세션에는 이 통로가 있다는 걸 알려준다.** 이 기능을 만든 이유가 에이전트가 옆방에 일을
  넘기는 자리인데, 사용법을 안 실으면 README 를 읽은 에이전트만 우연히 알게 된다. `ctb` 가 세션을
  띄울 때 사용법과 넘길 수 있는 방 목록을 붙인다 — **소켓이 실제로 있을 때만**이라 봇 없이 터미널만
  쓰면 토큰을 안 낸다. 자기 방은 목록에서 뺀다. 방에서 도는 에이전트에게는 안 준다(거기선
  `[[ctb-tell:]]` 마커의 몫이고, 길이 둘이면 모델이 갈린다).
  통로는 `.claude-bot/ctb.sock` (모드 0600, config 폴더 아래라 봇마다 갈린다). `"cliDispatch": false`
  로 소켓을 안 열 수 있다. 설계: [docs/design/cli-dispatch.md](docs/design/cli-dispatch.md).
- **`/sessions` 가 다른 역할의 세션을 이어받지 못하게 한다.** 세션 목록은 봇 상태가 아니라 디스크의
  jsonl 을 **프로젝트 폴더 기준**으로 훑어 만든다. 그래서 지금까지 한 프로젝트의 **모든 방 세션이 한
  목록에 섞여** 나왔고, 개발방에서 기획방 세션을 골라 그대로 이어갈 수 있었다. 역할이 붙은 뒤로는
  이게 "역할은 세션 경계에서만 정해진다"는 규칙의 **뒷문**이 된다 — 기획 맥락이 쌓인 세션을 개발
  역할 프롬프트로 이어받으면 정면 충돌이다.
  이제 세션마다 어느 역할로 만들어졌는지 적어 두고(`state.sessionPersona`, `/name` 의 `sessionNames`
  와 같은 자리), 목록에서 **다른 역할 세션을 뺀다.** 기록 지점은 `setSid` 한 곳이라 compact 로 세션
  ID 가 갈리는 경우까지 덮인다.
  역할이 생기기 전에 만들어진 세션은 **빼지 않고 ❓ 로 남긴다.** 빼 버리면 페르소나를 도입하는
  순간 이력이 통째로 사라진다. 그런 세션을 고르면 그 방의 역할로 기록되고, 되돌릴 수 없는 기록이라
  그 자리에서 한 번 알린다.
  목록 머리에도 지금 어느 역할 것을 보고 있는지 적는다. **`personas` 를 안 쓰면 목록도 예전
  그대로다** — 표 자체가 생기지 않는다.
  방별 페르소나의 4단계다 — 설계: [docs/design/room-personas.md](docs/design/room-personas.md).
- **방마다 다른 역할을 준다 (`personas` · `/persona`).** 지금까지 역할은 봇 하나에 하나였다
  (`cfg.persona`). 개발자와 기획자를 같이 두려면 config 를 나눠 봇을 둘 띄우는 수밖에 없었는데,
  그러면 **역할끼리 말을 주고받을 수가 없다** — 텔레그램 봇은 다른 봇이 보낸 메시지를 받지 못한다.
  이제 config 에 역할 **목록**을 두고 방(DM·그룹·포럼 토픽)마다 하나를 가리키게 한다. 봇 하나가
  개발방과 기획방을 동시에 맡고, 같은 봇이므로 `/tell` 로 역할 사이에 일을 넘길 수 있다.
  ```jsonc
  "personas": [
    { "id": "dev",     "name": "개발자", "prompt": "너는 시니어 개발자다. …" },
    { "id": "planner", "name": "기획자", "prompt": "너는 기획자 겸 UX 담당이다. …" }
  ]
  ```
  **역할은 세션이 사는 동안 고정이다** — 지금까지 오간 말이 전부 그 역할의 것이라, 도중에 정체성이
  바뀌면 쌓인 맥락과 모순된다. 그래서 고르는 자리는 세션 경계 셋뿐이고, 그 셋에서 봇이 버튼을
  띄운다: 방의 **첫 메시지** · **`/newchat`** · **`/new`**. 안 고르면 그냥 진행되고 목록의 첫 역할로
  돈다 — 폰에서 급히 물었는데 버튼부터 나오고 답이 안 오면 마찰이 크다. 첫 턴이 시작된 뒤에 뒤늦게
  누른 버튼은 저절로 무효가 된다(누르는 순간 세션을 다시 보기 때문이다).
  보는 자리는 둘로 나눴다 — `/status` 에 역할 이름 한 줄, `/persona` 에 **그 역할의 프롬프트 본문**
  (600자 넘으면 말줄임)과 다른 역할 이름들. 둘 다 바꾸지는 않는다.
  역할의 프롬프트뿐 아니라 **`/remember` 메모리도 그 역할 것**을 쓴다(`memory.dev.md`). `personas` 를
  처음 넣는 봇은 기존 `memory.md` 를 기본 역할 쪽으로 한 번 복사해 규칙이 조용히 빠지지 않게 한다.
  `id` 는 파일명이 되므로 `[a-z0-9-]` 만 받고, 중복·본문 없음과 함께 부팅 때 걸러내며 왜 뺐는지 로그에
  남긴다. `ctb` 도 방의 역할을 그대로 따라간다 — 여기가 어긋나면 터미널과 봇이 다른 역할로 돈다.
  **`personas` 가 없으면 지금까지와 한 줄도 다르지 않게 동작한다.**
  설계: [docs/design/room-personas.md](docs/design/room-personas.md).
- **`/remember` 메모리를 봇·페르소나별로 가른다.** `state.json` 은 config 이름에서 파생돼 안 섞이는데
  `memory.md` 는 그 규칙을 안 따라가서, **한 폴더에서 봇을 둘 띄우면 서로의 규칙을 주입받고 있었다**
  — 기획자 봇의 규칙이 개발자 세션의 `## RULES (must follow before anything else)` 로 들어가는
  식이다. 시스템 프롬프트 최상단이라 영향이 컸다. 이제 파일명이 `state` 와 같은 규칙으로 config
  이름에서 파생된다: `config.json` → `memory.md`, `planner.json` → `planner.memory.md`.
  **기본 config 를 쓰는 대다수 사용자는 경로도 동작도 그대로다.**
  이름 붙은 config 는 경로가 바뀌므로 첫 시작 때 옛 `memory.md` 를 새 경로로 **복사**하고 원본은
  남긴다 — 저 파일에는 여러 봇의 규칙이 섞여 쌓여 있어 어느 줄이 누구 것인지 코드가 가를 수 없다.
  양쪽에 같은 내용을 두고 각자 `/memory rm` 으로 지우는 쪽이 안전하다(지운 규칙은 다시 쓰면 되지만
  사라진 규칙은 사라진 줄도 모른다). 옛 봇이 아직 돌고 있어도 원본이 그대로라 안 깨진다.
  `ctb` 도 같은 규칙으로 경로를 고른다 — 여기가 어긋나면 터미널과 봇이 서로 다른 규칙을 읽는다.
  앞으로 한 봇이 방마다 다른 역할을 맡게 되면 페르소나 id 까지 파일명에 붙는다(`memory.dev.md`).
  방별 페르소나의 3단계다 — 설계: [docs/design/room-personas.md](docs/design/room-personas.md).
- **다른 방으로 메시지를 넘긴다 (`/tell <방> <메시지>`).** 0.4.3 부터 방마다 세션이 독립이라
  맥락이 안 섞이는 건 좋은데, 옆방이 알아낸 걸 가져올 통로가 없어 사람이 손으로 복사해 옮겨야 했다.
  이제 한 방에서 다른 방으로 메시지 하나를 넘길 수 있다. `/tell` 만 보내면 이 봇이 아는 방(= 한 번
  이라도 대화한 방) 목록이 나오고, 방은 **목록 번호나 이름의 일부**로 지목한다 — 토픽 이름은
  `큐브기획방 / 마케팅` 처럼 공백이 들어가서 전체를 치게 하면 폰에서 못 쓴다.
  전달된 메시지는 **대상 방의** 세션·provider·model·plan 고정을 그대로 따라 그 방에서 돈다. 보내는
  방의 설정은 하나도 넘어가지 않고, 대상이 바쁘면 그 방 대기열에 쌓인다.
  **답은 되돌아오지 않는다.** 되돌리는 순간 A → B → A 가 되고 그게 핑퐁의 시작이다. 사람은 어차피
  두 방을 다 본다.
  에이전트도 답변 끝에 `[[ctb-tell: 방 | 메시지]]` 를 붙여 넘길 수 있다(`[[ctb-image:]]` 와 같은
  방식). 이쪽만은 **대상 방에서 ✅ 를 눌러야** 실행된다 — 모델이 스스로 다른 방의 토큰을 쓰기
  시작하는 자리라서다. 여기에 **전달받은 메시지는 다시 전달할 수 없다는 규칙(홉 1)** 을 더하면,
  사람 없이 도는 전달 고리가 생기지 않는다. 마커를 버릴 때는 왜 안 갔는지 한 줄 남긴다.
  뮤트된 방(`/*`)에는 넘기지 않는다 — 뮤트는 "이 방은 지금 아무것도 실행하지 마라"는 뜻이다.
  시스템 프롬프트의 사용법 안내는 **넘길 방이 실제로 있을 때만** 붙어서, 방이 하나뿐인 봇은 이
  기능 때문에 토큰을 더 내지 않는다. `"roomRelay": false` 로 전체를 끈다.
  설계: [docs/design/room-relay.md](docs/design/room-relay.md).
- **plan 모드를 방에 고정한다 (`/plan on` · `/plan off`).** 지금까지 `/plan` 은 1회성이라
  "당분간 편집 없이 계획만 받자" 싶으면 **매 메시지마다 앞에 붙여야** 했고, 한 번이라도 빠뜨리면
  그대로 `permissionMode`(보통 `acceptEdits`·`bypassPermissions`)로 파일이 고쳐졌다 — 되돌리기
  어려운 쪽으로 실수가 나는 구조였다. 이제 켜 두면 끌 때까지 그 방의 모든 메시지가 계획부터 세우고
  같은 **✅ 진행 / ❌ 취소** 버튼과 함께 돌아온다. 인자 없는 `/plan` 은 현재 상태와 전환 버튼을 띄운다.
  `provider`·`model` 과 같은 자리(방별 설정)에 저장돼 재시작 후에도 유지되고, 포럼에서 한쪽 토픽만
  고정해 둘 수 있다.
  **승인 실행은 고정을 무시한다.** 이게 이 기능의 유일한 함정이다 — 승인한 계획을 또 plan 으로 돌리면
  승인 → 또 계획 → 승인 … 으로 영영 실행되지 않는다. 승인 직행 경로뿐 아니라 **작업 중에 승인을
  눌러 큐에 들어간 경우까지** 표시를 달아 걸러낸다(그쪽은 나중에 일반 메시지처럼 드레인되므로 표시가
  없으면 그대로 고정에 다시 걸린다). 다만 그 사이 사용자 입력이 섞여 병합됐으면 아직 승인 안 된
  요청이 함께 실행되는 셈이라, 그때는 계획부터 다시 세운다.
  **고정 중에는 Claude 한도 초과 시 Codex 로 폴백하지 않는다.** Codex 에는 plan 모드가 없어서,
  계획만 받으려던 요청이 한도에 걸렸다는 이유로 파일을 고치는 실행으로 바뀐다 — 권한이 조용히
  넓어지는 유일한 자리라 막고, 왜 건너뛰었는지 알린다. `/provider codex` 로 바꿔도 고정은 지우지
  않는다(방 설정이 조용히 사라지는 게 더 놀랍다). 대신 전환하는 자리에서 "Codex 에는 적용되지
  않는다"고 경고하고 `/status` 에도 그렇게 표시한다 — claude 로 돌아오면 다시 걸린다.
- **`ctb` 로 시작하면 어느 방을 이어받을지 고른다.** 화살표(또는 `1`~`9`)로 옮기고 엔터로 시작,
  `q` 로 취소한다. 방 키를 외워 `--chat` 에 넣는 건 사실상 불가능하고, 주제를 쓰기 시작하면 방이
  계속 늘어나서 목록을 한 번 보고 들어가는 게 기본이 되어야 했다.
  목록에는 이름과 함께 **방 키와 화이트리스트에서 빠진 방 표시**도 같이 띄운다 — `--chat` 에 넣을
  값을 볼 데가 여기 말고 없고, 그룹 승격으로 버려진 옛 채팅 ID 를 알아보는 것도 여기가 유일하다.
  **답이 이미 정해진 경우에는 뜨지 않는다** — 방이 하나뿐이거나, `--chat` 으로 지정했거나,
  `-p "..."` 처럼 인자를 넘겼거나, stdin 이 TTY 가 아닐 때다. 마지막 조건이 특히 중요한데,
  백그라운드 작업이나 파이프에서 프롬프트가 뜨면 물어볼 상대가 없어 그대로 멈춘다.
  화면은 stderr 에 그린다 — stdout 은 `-p` 의 답이 나가는 통로라 섞이면 안 된다. raw 모드로 둔 채
  죽으면 터미널이 먹통이 되므로 어느 경로로 끝나든(선택·취소·프로세스 종료) 되돌린다.
- **방 이름을 state 에 남긴다.** 위 목록의 전제다. 지금까지 `state.sessions` 에는 방 키(숫자)만
  있어서 어느 방인지 알아볼 방법이 없었다. 이제 봇이 그 방에서 응답할 때 텔레그램에 보이는 이름을
  같이 적어둔다 — DM 은 상대 이름, 그룹은 그룹 이름, 토픽은 `그룹 / 토픽` 이다. 토픽 이름은 일반
  메시지에 실려 오지 않고 **생성·이름 변경 서비스 메시지에만** 붙는데, 그 서비스 메시지는 본문이
  없어서 예전에는 빈 메시지로 취급돼 버려졌다. 이제 이름만 챙기고 그 자리에서 끝낸다. `/newchat`
  으로 만든 토픽은 만드는 순간 이름을 알 수 있어 그쪽에서도 따로 기록한다 — 서비스 메시지에
  `is_topic_message` 가 붙는지는 보장이 없어서 그 경로에만 기대지 않는다.
  **이미 있던 토픽은 서비스 메시지가 이미 지나간 뒤**라 이름을 얻을 통로가 없다. 답글 대상으로
  딸려 오는 생성 메시지(`reply_to_message.forum_topic_created`)가 유일한 뒷문이라 그것도 같이
  보고, 그마저 없으면 `봇 개발 / #11` 처럼 스레드 ID 로 적는다 — 그룹 이름만 적으면 General 과
  구별이 안 되고, 비워 두면 목록에서 무엇인지 알 방법이 없다.
  **이름의 출처는 신뢰도가 갈린다.** 서비스 메시지와 `/newchat` 은 그 순간의 진짜 이름이라 언제든
  덮어쓰지만, 답글에 딸려 오는 생성 정보는 **만들 당시 이름의 스냅샷**이라 그 뒤 바뀐 이름을 모른다.
  구분 없이 덮어쓰면 텔레그램에서 토픽 이름을 바꿔 반영해 놓아도 누군가 그 토픽 첫 메시지에 답장하는
  순간 옛 이름으로 되돌아간다. 스냅샷과 `#11` 은 아직 아는 이름이 없을 때만 쓴다.
- **연속해서 온 메시지를 합쳐 한 번만 대답한다 (`mergeWindowMs`, 기본 1초).** 사람은 한 생각을 여러
  메시지로 쪼개 보낸다 — "아까 그 버그 말인데" / "테스트부터 돌려봐" / "아 로그도". 지금까지는 첫 줄이
  도착하는 즉시 provider 를 띄웠기 때문에 뒤따라온 줄은 이미 시작된 작업 뒤에 줄을 섰고
  (`⏳ 대기열에 추가됐습니다`), 반쪽짜리 맥락으로 돌린 그 실행은 답까지 따로 와서 쓴 토큰과 시간이
  통째로 버려졌다. 이제 첫 메시지를 1초 붙잡아 두고, 그 사이 더 오면 창을 다시 연다. 조용해지면
  **작업 중 큐에 쓰던 `drainQueue()` 를 그대로 재사용해** 한 덩어리로 합쳐 한 번만 실행한다 —
  `[1]` / `[2, +3s]` 번호·경과시간 라벨, 그룹이면 발신자 이름, 앞 메시지의 첨부까지 이미 챙기는 코드다.
  창을 붙잡아 두는 곳도 별도 버퍼가 아니라 기존 방 대기열이라 `/stop`·`/restart`·`/reserve` 가 세는
  "대기 중인 메시지"가 한 곳으로 유지된다.
  붙잡는 자리는 명령어 처리가 **전부 끝난 뒤**라 `/status`·`/stop`·`/model` 은 창과 무관하게 즉시
  응답하고, 바쁘거나 로컬 락이 걸린 경우도 예전처럼 각자의 안내를 바로 받는다. 창이 열리는 순간
  타이핑 표시를 한 번 띄워 1초의 침묵이 "봇이 죽었나"로 보이지 않게 했다. 말이 계속 이어지면 창이
  무한정 밀리므로 첫 메시지 기준 5배(기본 5초)에서 끊고 시작한다. `"mergeWindowMs": 0` 이면 예전처럼
  메시지마다 즉시 실행한다.
  **길이는 `/mergewindow` 로 재시작 없이 바꾼다** — 인자 없이 보내면 현재값과 프리셋 버튼
  (0.5s / 1s / 2s / 3s / 5s / 끄기 / 기본값)이 뜨고, `/mergewindow 2s`·`2000`·`off`·`default` 도
  받는다. 손에 맞는 길이는 두 줄째를 치는 속도에 달려 있어서 몇 번 바꿔봐야 나오는데, config 에만
  두면 그때마다 재시작이라 결국 안 만지게 된다. 0.1초~30초 밖은 거절한다 — 너무 짧으면 있으나
  마나고, 너무 길면 한 줄만 보낸 사람 눈에 봇이 죽은 것으로 보인다. 설정값은 `state.json` 에 남는다.
  **붙잡아 둔 메시지도 `state.json` 에 같이 적어 두고 다음 기동 때 이어서 실행한다.** 창이 열려 있는
  동안 재시작하거나 프로세스가 죽으면 그 메시지는 메모리와 함께 통째로 사라지는데, 대기열에 밀린
  메시지와 달리 `⏳` 안내조차 나가지 않은 상태라 보낸 사람은 계속 답을 기다린다. 텔레그램 Bot API 는
  이미 받아간 메시지를 되가져올 수단이 없어서 — `getUpdates` 는 아직 안 가져간 업데이트만, 그것도 한
  번만 준다 — 여기서 흘리면 복구할 방법 자체가 없다.
  레이트리밋 중에 창이 닫히는 경우도 마찬가지로 조용했다. 큐에 그대로 두고 해제될 때 드레인하는
  동작 자체는 맞지만 안내를 한 번도 보내지 않아서, 보낸 사람 눈에는 무시당한 것과 구분되지 않았다.
  이제 대기 중이라는 사실과 해제 예정 시각을 알린다.
- **새 그룹에 초대하면 봇이 넣어야 할 채팅 ID 를 알려준다.** 지금까지 `allowedChatId` 에 없는 방의
  메시지는 로그에만 남기고 조용히 버렸다. 초대한 사람 눈에는 봇이 죽은 것과 구분이 안 되고, 넣어야 할
  chatId 를 알아낼 방법도 없었다 — `/id` 조차 화이트리스트 뒤에 있어서 답이 없다. 이제 모르는 방에서는
  그 방의 chatId, 넣을 설정 파일 경로, 붙여넣을 JSON 조각을 한 번에 보낸다. 초대되는 순간
  (`my_chat_member`) 먼저 말하는데, 이 업데이트는 privacy mode 와 무관하게 항상 오기 때문에 **일반
  대화가 봇에 아예 닿지 않는 방에서도** 안내가 뜬다. **방 하나당 딱 한 번만** 말한다 — 아무나 봇을
  그룹에 끌어다 넣을 수 있어서, 메시지마다 답하면 봇이 스팸 중계기가 된다. 안내를 보내도 그 방에서
  명령을 실행하지는 않는다. 설정 파일 경로에는 계정 이름이 묻어 있어서, 허용된 방의 주인이 부른 게
  아니면 실제 경로 대신 `config.json` 이라고만 알린다.
- **[그룹에서 봇 쓰기](docs/group-setup.ko.md) 안내 페이지 (영/한).** BotFather `/setprivacy` 를
  Disable 하는 이유와 방법 — privacy mode 가 켜져 있으면 명령·멘션·답장만 봇에 전달돼서 평범한 대화가
  통째로 사라지는데, 로그에도 안 남아 원인을 찾기 어렵다. 설정을 바꾼 뒤에는 **이미 들어가 있는 방에서
  한 번 내보냈다 다시 초대해야** 반영된다는 것까지 적었다. 그 밖에 주제(Topics) 켜기와 *주제 관리*
  권한, 주제를 켜면 채팅 ID 가 바뀌는 이유, 방 단위 화이트리스트의 위험, 증상별 문제 해결 표를 담았다.
### Fixed
- **`/new` 가 실행 중에는 무효가 되던 문제.** `/new` 는 돌고 있어도 먹혀야 해서 busy 검사보다 앞에서
  처리되는데, 그때 돌던 실행이 끝나면서 **후속 세션 ID 를 도로 등록해** 방금 버린 맥락이 그대로
  되살아났다. 새 대화라고 안내받은 채 옛 맥락 위에서 계속 말하게 되는 셈이다. 방 런타임에
  세대(`gen`)를 두고, 실행을 시작한 시점과 다르면 결과 세션을 버린다. 돌던 답장 자체는 그대로
  보낸다 — 실행 취소는 `/stop` 의 일이고 `/new` 는 "다음 메시지부터 새로"라는 뜻이다. 압축·계획
  승인·폴백처럼 세션 ID 를 새로 얻는 경로에도 같은 규칙을 걸었다.
- **`/new` 뒤에도 폴백이 옛 대화를 기억하던 문제.** `/new` 가 **현재 provider 세션만** 지워서 Codex
  세션이 살아남았고, 다음 레이트리밋 폴백이 그 세션을 그대로 이어받았다. 맥락을 비웠다고 안내해
  놓고 폴백만 옛 대화를 아는 상태였다. 이제 양쪽 provider 세션을 다 지운다 (다른 방은 그대로).
- **긴 로그를 붙여넣으면 첫 조각만 혼자 실행되던 문제.** 텔레그램 상한(4096자)을 넘는 텍스트는
  보내는 쪽 클라이언트가 알아서 조각내 보내는데, **첫 조각만 나머지보다 1초 넘게 먼저 도착한다** —
  클라이언트가 첫 조각의 ack 를 받고서야 나머지를 몰아 보내기 때문이다. 그래서 기본 1초 창은 첫
  조각에서 닫혀 버리고, 정작 내용이 담긴 뒤 조각들은 이미 시작된 실행 뒤로 밀렸다. 실제 기록에서
  8조각짜리 로그의 2~8번은 서로 `+0s` 였는데 1번만 창 밖이었고, 봇은 잘린 앞부분만 읽고 한 번,
  나머지 27KB 를 읽고 또 한 번 답했다 — 첫 답은 문장 중간에서 끊긴 JSON 을 받은 셈이라 통째로
  버려진다. 조각에는 "이어짐" 표시가 없지만 길이로 알아볼 수 있다(관측한 조각은 `-0.` 처럼 숫자
  중간에서 끊겨 있었다). 이제 길이가 상한에 붙어 도착한 메시지는 뒤가 더 온다고 보고 창을 최대
  8초로 늘려 잡는다. **마지막 조각은 짧아서 창이 곧바로 기본값으로 돌아온다** — 붙여넣기가 끝나면
  더 기다리지 않고, 보통 메시지의 1초도 그대로다.
- **승인 대기 중에 자동 압축을 묻던 문제.** 계획을 받고 ✅ 를 누르기 전까지가 사람이 가장 오래
  머무는 자리인데, 하필 그 위에 "지금 압축할까요?" 가 겹쳐 떴다. 압축은 세션 ID 를 갈아치우므로
  그쪽을 먼저 누르면 승인하려던 계획이 그 자리에서 만료되고(`승인할 계획이 없습니다`), 계획을
  다시 세우느라 방금 압축으로 아낀 것보다 더 쓴다. plan 을 고정해 두면 매 턴 승인이 뜨므로 상시
  함정이 된다. 이제 승인이 떠 있거나 이 답 바로 뒤에 붙을 참이면 물음을 미뤄 두고, 계획이
  취소·만료된 뒤에 꺼낸다 — 승인해서 실행된 경우는 그 실행이 끝나며 스스로 다시 판단한다.
  `autoCompactConfirm: false` 로 묻지 않고 바로 압축하는 설정도 같은 이유로 미룬다(이쪽이 오히려
  조용히 승인을 죽인다). `/new` 로 컨텍스트를 비우면 미뤄둔 물음도 같이 버린다.
  손으로 부른 `/compact` 는 그대로 돌되, 대기 중이던 계획이 만료됐다고 알린다 — 예전에는 나중에
  버튼을 눌러 보고서야 알았다.
- **포럼 그룹의 General 토픽에서 타이핑 표시가 안 보이던 문제.** General 은 메시지에
  `is_topic_message` 가 붙지 않아 방 키에 토픽 접미사가 없고, 그래서 `sendChatAction` 도 스레드를
  적지 않고 나갔다. 메시지 전송은 그래도 General 에 잘 들어가기 때문에 이 표시만 티가 안 났는데,
  실제로는 그룹의 "모두" 뷰에만 뜨고 정작 General 안에서는 아무것도 보이지 않았다 — 긴 작업이
  돌고 있어도 방은 조용해 보였다. 이제 포럼으로 확인된 방이면 General 의 스레드 ID(`1`)를 붙여
  보낸다. 포럼이 아닌 방(DM·일반 그룹)에는 예전 그대로 붙이지 않는다.
- **긴 작업 중에 타이핑 표시가 주기마다 끊기던 문제.** 텔레그램의 타이핑 표시는 보낸 지 5초면 스스로
  꺼지는데 갱신 주기도 정확히 5초였다. 왕복 지연이 수백 ms 만 있어도 갱신이 만료 뒤에 도착해서 주기마다
  표시가 잠깐씩 사라졌고, 오래 걸리는 작업일수록 "돌고는 있나" 싶게 보였다. 이제 4초마다 갱신해 만료
  전에 덮어쓴다.
- **작업이 시작되기 전의 `/stop` 이 먹지 않던 자리.** 병합 창이 열려 있는 동안 `/stop` 을 치면
  실행 중인 작업이 없다는 이유로 `중단할 작업이 없습니다` 만 답하고 큐를 비우지 않아서, 1초 뒤에
  그 작업이 그대로 시작됐다. 이제 대기 중인 창을 취소하고 큐를 비운다.
- **첨부 다운로드가 실패하면 빈 프롬프트로 세션이 돌던 문제.** 20MB 넘는 파일처럼 Bot API 로 받을 수
  없는 첨부를 캡션 없이 보내면, `⚠️ 첨부 파일 처리 실패` 를 보낸 뒤에도 실행을 멈추지 않았다. 본문이
  비어 있어도 그 뒤에 붙는 `[From: 이름]` 한 줄 때문에 프롬프트가 non-empty 가 되어 빈 요청 가드를
  그대로 통과했고, 에이전트는 발신자 이름 한 줄만 받아 "메시지가 비어서 왔습니다" 같은 답을 하며
  턴과 토큰을 버렸다. 이제 받아낸 첨부가 하나도 없고 본문도 비면 실행하지 않는다. 캡션이 있으면
  캡션만으로 실행하되 첨부를 받지 못했다는 사실을 프롬프트에 한 줄 남긴다.
- **첨부 실패 사유가 뭉개지던 자리.** `getFile` 이 실패하면 이유와 무관하게 전부 `getFile failed` 로
  보고해서 용량 초과인지 네트워크 문제인지 구분되지 않았다. 이제 텔레그램이 준 `description`
  (예: `Bad Request: file is too big`) 을 그대로 싣는다.
### Changed
- **로컬 `ctb` 세션은 그 방만 잠근다.** 터미널에서 세션을 붙잡고 있으면 봇이 **모든 방**의 메시지를
  미뤘다. 방마다 세션이 따로고 토픽끼리도 병렬로 도는데 락만 봇 전체를 잠근 셈이라, DM 에서 터미널
  작업 중이라는 이유로 그룹 토픽까지 멈춰 섰다 — 막을 이유가 없는 조합이다. 락이 존재하는 이유는
  같은 세션을 양쪽에서 동시에 `--resume` 하는 걸 막기 위해서고, 그건 애초에 방 단위 문제다.
  이제 `ctb` 가 lock 파일에 잡고 있는 방을 같이 적고, 봇은 그 방만 미룬다. `/local` 과 `/status` 는
  어느 방인지 이름으로 알려주고 다른 방은 그대로 돈다고 덧붙인다.
  **파일 형식은 첫 줄 PID, 둘째 줄 JSON 으로 나눴다.** 통째로 JSON 을 적으면 0.4.13 이전 봇이
  `parseInt` 로 읽어 `NaN` 을 얻고, 그 경로는 "죽은 lock" 으로 판정해 **파일을 지워 버린다** —
  잠금이 아예 풀린다. 옛 봇은 첫 줄만 읽고 새 봇은 둘째 줄까지 읽어서, 어느 조합이든 안전하다.
  반대로 옛 `ctb`(PID 한 줄)가 만든 락은 방을 알 수 없으니 예전처럼 전 방을 잠근다.
  예약 작업은 방이 아니라 전용 슬롯에서 돌기 때문에, 어느 방의 로컬 세션이든 있으면 예전처럼
  건너뛴다.
- **provider와 모델을 방별로 분리.** `/provider`와 `/model` override를
  `state.sessions[roomKey]`에 저장해 DM·그룹·포럼 토픽이 서로 다른 Claude/Codex와 모델을 쓸 수 있다.
  기존 전역 override는 알려진 방들로 자동 이관하고, `/provider default`·`/model default`는 현재 방만
  config 기본값으로 돌린다. `ctb --chat`도 선택한 방의 provider와 모델을 따른다. Claude 한도 대기
  중이어도 Codex 방은 해당 전역 대기열에 묶이지 않는다.
- **받을 업데이트 종류(`allowed_updates`)를 명시한다.** 텔레그램은 이 값을 생략하면 그 토큰에 대해
  '직전에 지정한 값'을 계속 쓴다. 같은 토큰을 예전에 다른 도구가 좁혀 놨다면 `my_chat_member` 가
  조용히 안 와서 초대 안내가 통째로 사라진다. 이제 `message`·`callback_query`·`my_chat_member` 를
  매번 적어 보낸다 — 이 봇이 처리하는 전부다.

## [0.4.12] - 2026-08-21
### Added
- **포럼 주제(토픽)마다 방 하나 — `/newchat`.** 텔레그램 봇 API 로는 그룹을 만들 수 없다
  (그룹 생성은 유저 계정 전용 MTProto 기능이다). "새 그룹에서 새 대화"에 가장 가까운 건 주제가
  켜진 슈퍼그룹의 **토픽**이고, 그건 봇이 `createForumTopic` 으로 직접 만들 수 있다.
  `/newchat` 은 토픽을 만들고 거기서 새 세션으로 시작한다 (`/newtopic` 도 같은 명령이다). `/newchat 리팩토링` 처럼 이름을
  주면 그 이름으로, 안 주면 `새 대화 08-19 17:30` 처럼 날짜·시각으로 붙는다. 주제가 안 켜져
  있거나 봇에 '주제 관리' 권한이 없으면 텔레그램이 준 사유를 그대로 보여주고 뭘 켜야 하는지 알린다.
  DM 에는 토픽 개념이 없어서 거기선 `/new` 로 안내한다.
### Changed
- **설정 메뉴 버튼은 다음 입력이 오면 사라진다.** `/model`·`/provider`·`/autocompact`·`/sessions`
  와 자동 압축 물음의 인라인 버튼은 '보낸 시점의 상태'를 그린 스냅샷인데, 대화가 이어져도 위로
  밀려 올라갈 뿐 계속 눌리는 상태로 남아 있었다. 스크롤을 올려 옛 `/model` 메뉴를 누르면 그 사이
  바꿔둔 모델이 조용히 되돌아가고, 화면에는 "설정 완료"만 떠서 왜 바뀌었는지 알 수 없었다. 이제
  방마다 살아 있는 메뉴는 하나만 두고, 그 방에 새 입력이 오면 이전 메뉴의 버튼을 걷어낸다. 방 단위라
  다른 토픽의 메뉴는 건드리지 않고, 주석(`//`·`/* */`)으로 무시한 메모는 대화가 아니라 메뉴를
  걷어내지도 않는다. 아직 답을 안 받은 `/plan` 승인과 `/local` 종료 버튼은 그대로 남긴다 —
  그 둘은 눌러야 끝나는 요청이고, 눌린 뒤에는 원래대로 버튼이 지워진다.
### Fixed
- **iOS 가 `--` 를 em 대시로 바꿔 플래그가 통째로 새던 문제.** iOS 키보드의 스마트 문장부호는
  하이픈 둘을 `—` 하나로 합친다. 그래서 `/new --chat test` 가 `/new —chat test` 로 도착하고,
  어느 명령에도 안 걸려서 그대로 Claude 프롬프트로 흘러갔다. 하필 Claude 쪽 `/new` 는 `/clear`
  별칭이라 세션이 조용히 날아가고 봇은 `(empty response)` 만 찍었다. 이제 명령어 메시지에 한해
  `—`/`–` 뒤에 소문자 ASCII 가 붙은 것만 `--` 로 되돌린다 — `/remember 회의 — 결론` 같은 본문
  대시는 그대로 둔다. 애초에 대시를 안 쓰도록 `/newchat`(=`/newtopic`) 을 별도 명령으로
  뺐고, 옛 `/new --chat` 도 계속 받는다.
- **그룹이 슈퍼그룹으로 승격되면 봇이 조용히 죽던 문제.** 일반 그룹에서 '주제(Topics)'를 켜면
  텔레그램이 그 방을 슈퍼그룹으로 올리면서 채팅 ID 를 새로 발급한다(`-100…`). 그러면
  `allowedChatId` 의 옛 ID 는 죽은 값이 되고, 봇은 새 방의 메시지를 전부 "모르는 방"이라며
  로그에만 남기고 버렸다 — 쓰는 사람 눈에는 봇이 죽은 것과 구분이 안 됐다. 이제 승격 시
  텔레그램이 보내주는 `migrate_to_chat_id`/`migrate_from_chat_id` 를 받아, **옛 ID 가 허용된
  방이었을 때만** 새 ID 를 물려받고(`state.adoptedChatIds`) 그 방의 세션·토픽 세션 키까지 새
  ID 로 옮긴다. 새 방에 ID 가 바뀌었다고 알리고 `config.json` 갱신을 권한다. 허용된 방에서
  출발한 승격만 따라가므로 아무 그룹이나 스스로 화이트리스트에 들어올 수는 없다.
- **포럼 그룹에서 토픽이 전부 한 세션을 공유하던 문제.** `message_thread_id` 를 읽지도 쓰지도
  않아서, 토픽을 나눠 써도 맥락이 한 통에 섞이고 답장은 죄다 General 로 갔다. 이제 방 키를
  `chatId` 에서 `chatId:토픽ID` 로 넓혀 토픽마다 세션·대기열·실행 상태를 따로 들고, 답장·타이핑
  표시·사진도 온 토픽으로 돌려보낸다. 토픽이 아닌 방(DM·일반 그룹·General)의 키는 예전 그대로
  `chatId` 라 `state.json` 마이그레이션이 필요 없다.
  일반 슈퍼그룹도 답글에 `message_thread_id` 를 붙이기 때문에, 포럼 토픽을 뜻하는
  `is_topic_message` 가 있을 때만 방을 나눈다 — 안 그러면 답글 하나마다 세션이 쪼개진다.
  화이트리스트(`allowedChatId`)·리액션·버튼 수정처럼 토픽과 무관한 자리는 계속 실제 채팅 ID 를
  쓴다. 즉 그룹을 한 번 허용하면 그 안의 토픽은 따로 등록하지 않아도 된다.

## [0.4.11] - 2026-08-14
### Added
- **`/sessions` — 지난 세션 목록에서 골라 이어가기.** Claude·Codex 모두 대화를 jsonl 로 남기고
  파일명·메타에 세션 ID 가 들어 있어서, 목록을 버튼으로 띄우고 고른 것을 그 방의 세션으로
  갈아끼운다(이미 `--resume`/`exec resume` 을 쓰고 있어 이어받는 경로는 그대로다).
  목록은 **항상 현재 provider 기준** — Claude 세션과 Codex 세션은 호환되지 않아 섞어 보여주면
  고르는 순간이 사고다. **다른 방이 쓰는 세션(🔒)과 터미널에서 열려 있는 세션(💻)은 막는다** —
  한 기록 파일에 두 프로세스가 붙으면 서로의 맥락을 덮어쓴다. 다른 방은 `state.json` 으로 알지만
  터미널은 거기 안 남아서, 목록을 그릴 때 `ps` 로 `claude --resume`·`codex exec resume` 을 훑는다
  (실패하면 조용히 넘어간다 — 표시가 없을 뿐 목록은 그대로 뜬다). 이미 쓰고 있는 세션(✅)을 다시
  고르면 "바꿨다" 대신 **아무 일도 안 일어났다**고 알린다. 정렬은 만든 시각이 아니라 **마지막 사용 시각**이다;
  `resume` 은 원본 파일에 덧붙이므로 7월에 만든 세션이 어제 쓴 세션일 수 있다.
  Claude 기록 폴더는 프로젝트 경로를 인코딩한 이름이라 규칙이 바뀌면 못 찾을 수 있어서,
  이름으로 먼저 찾고 실패하면 jsonl 안의 `cwd` 로 되짚는다. 14MB 짜리 기록도 있어 통째로 읽지
  않고 앞뒤만 잘라 읽는다. 버튼에 붙는 미리보기는 첫 메시지가 아니라 **마지막 사용자 메시지**다 —
  며칠 이어온 세션은 뭘로 시작했는지보다 뭘 하다 말았는지가 알아보기 쉽고, 옆의 시각과도 같은
  시점을 가리킨다.
  목록은 **한 번 쓰고 마는 확인 버튼이 아니라 메뉴다** — 하나 골라도 버튼이 남고, 되돌아가서
  다시 고를 수 있다(중복 클릭 방지 잠금은 세션 목록만 예외로 뒀다; 전환은 몇 번 눌러도 결과가
  같다). 고를 때마다 목록의 ✅ 를 그 자리에서 다시 그린다.
- **`/name` — 세션에 이름 붙이기.** 미리보기는 마지막 메시지라 대화가 이어지면 계속 바뀐다.
  오래 붙잡아 둘 세션은 `/name 톰` 처럼 직접 이름을 달아두면 `/sessions` 목록에 🏷 로 뜬다.
  이름은 방이 아니라 세션 ID 에 붙어서, 다른 방이 그 세션을 이어받아도 같은 이름으로 보인다.
- **백그라운드 작업이 답장과 함께 죽던 문제 — `.ctb-jobs` 와 `/jobs`.** 에이전트는 메시지마다 새
  프로세스로 떴다가 답장을 보내면 종료된다. 그때 에이전트가 `run_in_background` 로 띄운 작업도 같이
  죽는다(실측 확인). 개발 서버나 긴 빌드를 시켜놓고 답장을 받으면 그 작업은 이미 없었다.
  이제 **답장보다 오래 살아야 하는 작업**은 `nohup … & disown` 으로 프로세스 그룹 밖에 내보내고
  `.ctb-jobs/` 에 기록(`<이름>.json` + `<이름>.log`)을 남긴다. 봇은 30초마다 `kill(pid, 0)` 으로
  **생사만** 확인하고, 끝나면 로그 꼬리와 함께 먼저 알린다. `/jobs` 로 목록을 본다.
  핵심은 **봇이 작업을 소유하지 않는다**는 것이다 — 자식으로 띄우면 `/restart` 한 번에 돌던 작업이
  전부 죽어서 배포 수단이 작업 학살 수단이 된다. 소유하지 않으니 감시자가 무상태이고, 봇이 다시
  뜨면 폴더만 다시 읽어 감시를 이어간다(꺼져 있는 동안 끝난 작업도 부팅 직후 회수한다).
  규칙은 스킬이 아니라 **시스템 프롬프트**로 싣는다. 스킬은 에이전트가 관련 있다고 판단해야
  로드되는데, 안 걸리면 작업이 죽는 그 버그가 그대로 남는다. 항상 켜져 있어야 하는 규칙이다.
  다만 **무조건 금지는 아니다** — 답장 전에 결과를 읽어볼 작업이면 `run_in_background` 가 맞고,
  로컬 `ctb` 대화형 세션에서는 그쪽이 더 낫다. `ctb` 도 방 번호를 넘겨서, 텔레그램에서 띄우든
  터미널에서 띄우든 같은 기록이 남고 같은 방으로 알림이 간다. `"backgroundJobs": false` 로 끈다.
- **`/* … */` 블록 주석 — 여러 메시지를 한 번에 무시.** `//` 는 한 줄짜리라, 로그를 붙여넣거나
  메모를 연달아 남길 때 매번 붙여야 했다. `/*` 를 보내면 `*/` 로 시작하는 메시지가 올 때까지
  그 방의 입력을 전부 무시한다. 방별로 저장하고 재시작해도 유지한다 — 재시작이 쌓인 메모를
  세션에 쏟아붓는 쪽이 더 나쁜 사고다. 닫는 걸 잊으면 봇이 죽은 것처럼 보이는 게 유일한 함정이라,
  들어갈 때 탈출 방법을 알리고 무시하는 메시지마다 👀 를 남긴다. `/* 메모 */` 처럼 한 메시지에서
  열고 닫으면 블록에 들어가지 않고 1회성 무시로 끝난다.
- **`/memory rm <번호>` — 메모리 한 줄만 삭제.** 지금까지는 `/memory clear` 로 통째로 비우고
  남길 것을 `/remember` 로 다시 넣는 수밖에 없었다. `/memory` 목록에 번호가 붙어서 그 번호로 지운다.
  여러 개·범위도 한 번에 받는다 — `/memory rm 3 5 7`, `/memory rm 3-9`, 섞어 써도 된다. 정리는
  몰아서 하게 되는 일이라 하나씩 지우면 번호가 매번 밀려 목록을 다시 봐야 한다.
  **범위의 끝은 목록 끝으로 줄여서 받는다** — 7개인데 `3-9` 를 쳤으면 3번부터 끝까지 지우려는
  것이지 오타가 아니다(`sed -n '3,9p'` 와 같은 감각). 반면 없는 번호를 콕 집은 단건(`rm 9`)이나
  시작이 목록 밖인 범위(`rm 9-12`), 거꾸로 된 범위(`rm 5-3`)는 의도를 짐작할 수 없어 거절하고,
  이때는 **아무것도 지우지 않는다** — 일부만 지우고 나면 남은 번호가 어긋나서 이어 친 명령이
  엉뚱한 규칙을 지운다. 삭제 응답에는 **진행 중인 대화에는 한동안 남는다**는 안내를 붙였다 (`/memory clear` 도 동일) —
  지우고 나서도 봇이 계속 그 규칙을 따르는 것처럼 보여 버그로 오해하기 쉬운 자리다.
- **메모리가 8개를 넘으면 `/remember` 응답에 정리 권유.** 문제는 토큰이 아니라 주목도다 —
  `RULES` 블록에 스무 줄이 쌓이면 신호가 평평해져 **기존 규칙까지 약해진다.** 눈치채기 어려운
  손해라서 늘어나는 시점에 알린다.
- **Codex `/model` 선택 안내.** 설치된 Codex CLI의 계정별 모델 캐시를 읽어 선택 가능한 모델을
  버튼으로 표시한다. 목록 밖의 ID도 **막지 않고 설정하되 경고를 붙인다** — 캐시는 CLI 가 갱신하는
  파일이라 새로 나온 모델을 아직 못 받았을 수 있고, 막아버리면 정당한 모델을 텔레그램에서 고를 수
  없게 된다. 캐시가 없는 환경에서는 기존처럼 전체 모델 ID 직접 입력을 허용한다.

### Fixed
- **`ctb` 로컬 세션에 persona 와 `/remember` 규칙이 안 붙던 문제.** `ctb` 는 `claude` 에
  `--resume` 만 넘기고 `--append-system-prompt` 는 아예 안 넘겼다. 이어받은 세션은 지난 대화를
  흉내내서 persona 가 남아 있는 것처럼 보이지만(`--append-system-prompt` 는 호출마다 주는 값이라
  기록에 저장되지 않는다), 텔레그램에서 `/new` 하고 아무것도 안 친 상태로 `ctb` 를 띄우면
  이어받을 대화가 없어서 규칙이 통째로 빠진 맨 `claude` 가 떴다. 이제 두 경우 모두 메모리
  RULES 블록과 persona 를 붙인다 — 순서(메모리 먼저)와 헤더까지 `bot.mjs` 와 같게 맞췄다.
  붙이는 건 설정·파일에서 그대로 오는 것뿐이고 텔레그램용 문구(간결한 답변, 이미지 전송 규약,
  모델 상향 권유)는 터미널에 해당하지 않아 뺐다. Codex 는 `--append-system-prompt` 가 없어
  대화형에서 끼워 넣을 자리가 없다 — 여전히 미적용이다.
- **Codex 세션을 이어갈 때 `/remember` 규칙이 빠지던 문제.** Codex 에는 `--append-system-prompt` 가
  없어 프롬프트 앞에 붙이는데, 그걸 **새 세션에서만** 했다. 그래서 세션이 시작된 뒤에 `/remember` 로
  추가한 규칙은 그 세션에 영영 들어가지 않았고, 같은 규칙이 Claude 에서는 먹고 Codex 에서는 안 먹는
  것처럼 보였다. 이제 이어가는 세션에도 매 턴 메모리를 다시 싣는다 (persona 등 나머지는 세션 시작
  때 한 번만 — 중복해서 실을 이유가 없다).
- **ctb 가 세션에 끼워 넣는 마커가 `-` 로 시작해서 `claude` 가 죽던 문제.** 마커를 `-p` 의 값으로
  넘기는데 `---ctb:start---` 처럼 `-` 로 시작하면 인자 파서가 값이 아니라 **옵션으로 읽고**
  `error: unknown option '---ctb:start---'` 로 종료한다. 시작 마커는 처음부터 그랬고
  (`stdio` 를 전부 `ignore` 로 두고 종료 코드도 안 보는 자리라 아무도 몰랐다), 이번에 같은 규칙을
  인수인계 프롬프트에도 쓰면서 **인수인계가 통째로 실패**했다 — 위 항목에서 SKIP 으로 위장되던 게
  이것이다. 마커를 `<ctb:start>` · `<ctb:handoff>` 로 바꿨다. `<` 는 취향이 아니라 조건이다.
  `bot.mjs` 의 `/sessions` 미리보기 필터도 같이 고쳤다(옛 문구 두 줄은 지난 기록용으로 남겨둠).
- **인수인계가 실패해도 "넘길 게 없음(SKIP)"으로 뜨던 문제.** `summarizeSession` 은 결과를 `null`
  하나로만 돌려줬다. 모델이 정말 `SKIP` 이라고 답한 경우, 30초 타임아웃에 잘린 경우, `claude` 가
  0 아닌 코드로 죽은 경우, 출력이 JSON 이 아닌 경우가 **전부 같은 `null`** 이라 화면에는 똑같이
  `nothing to hand over (SKIP)` 로 떴다. 게다가 자식 프로세스의 stderr 를 `ignore` 로 버려서
  진짜 이유가 아무 데도 안 남았다 — 실제로 이 세션에서 인수인계가 두 번 실패했는데 SKIP 으로
  보여서, 기록(jsonl)에 프롬프트 턴이 안 붙은 걸 확인하고서야 실패인 줄 알았다. 이제 결과를
  `{ text }` · `{ skip }` · `{ error }` 로 갈라서, 실패는 `ctb: handoff failed — <이유>` 로 찍는다.
  stderr 도 받아서 마지막 줄을 이유에 붙인다. 타임아웃은 **30초에서 180초로** 늘리고
  `ctbNotifyTimeout` 으로 뺐다 — 17MB 짜리 세션은 이어받아 첫 응답까지만 12초가 걸려서
  30초는 될 일도 자르는 값이었다.
- **`init` 이 없는 폴더를 가리키면 스택 트레이스를 뱉고 죽던 문제.** README 빠른 시작의 첫 명령이
  `init ~/botconfigs/my-project` 인데, 그 폴더는 아직 없는 게 정상이라 처음 설치한 사람이 그대로
  따라 하면 `ENOENT` 로 터졌다. 이제 상위 폴더를 만들고 진행한다.

### Changed
- **`ctb` 종료 알림이 요약에서 인수인계로.** 예전 프롬프트는 "무엇을 했는지 10단어 이내로
  요약해줘" 였다. 그건 사후 기록이지 인수인계가 아니다 — 대화는 끝나는 게 아니라 텔레그램으로
  자리를 옮기는 것이므로, 이어받는 쪽에 필요한 걸 물어야 한다. 이제 "지금 이 세션을 끝내고 같은
  사람과 텔레그램에서 이어갑니다. 넘길 말이 있으면 알려주세요 — 끝내지 못한 것, 확인이나 결정이
  필요한 것, 주의할 점" 으로 묻고 3줄까지 받는다. 넘길 게 없으면 `SKIP` 으로 물러서는 건 그대로다.
- **`ctb` 종료 알림이 이어받은 방에만 간다.** `allowedChatId` 전부에 뿌리고 있었다. 방마다 세션이
  갈리는데 알림만 안 갈려서, DM 세션을 붙잡고 일한 내용이 그룹방에도 그대로 떴다. 이제 이어받은
  그 방(`--chat` 으로 고른 방, 없으면 `allowedChatId` 첫 번째)에만 보낸다. 화이트리스트 밖의
  방이면 첫 방으로 물러선다 — 봇이 서비스하지 않는 방으로 세션 내용을 보내지 않기 위해서다.
  시작할 때 찍는 줄에도 방을 같이 넣었다(`Resuming … (chat 111)`) — 전에는 세션 ID 만 떠서 DM 을
  이어받았는지 그룹을 이어받았는지 알 수 없었다.
- **ctb 가 세션에 끼워 넣는 턴에 `---ctb:` 접두사.** `/sessions` 미리보기가 걸러내야 하는
  대상인데, `bot.mjs` 가 옛 영문 프롬프트를 **문자열 그대로** 물고 있어서 문구를 바꾸는 순간
  조용히 깨졌다(목록에 인수인계 프롬프트가 미리보기로 뜬다). 시작 마커(`---ctb:start---`)와
  인수인계 요청(`---ctb:handoff---`)이 같은 접두사를 쓰고 봇은 그것만 본다. 지난 기록에는 옛
  문구가 남아 있어 그쪽 규칙도 함께 남겼다.
- **README 빠른 시작을 `ctb` 기준으로.** 전역 설치하면 `ctb` 도 같이 깔리므로 더 짧다.
  데몬은 `ctb bot <config>` — `bot` 없이 쓰면 데몬이 아니라 로컬 터미널 세션이 뜬다는 주의를 함께 적었다.
- 한국어 README 명령어 표 아래에 실제 화면 추가 — `/remember` 로 출력 형식을 규칙으로 걸어 두면
  이후 답변부터 그 형식으로 나오는 흐름. 파일을 못 쓰는 `plan` 봇이 규칙을 얻는 유일한 경로다.
- README(영/한)에 **규칙을 지워도 진행 중인 대화에는 한동안 남는다**는 설명 추가. `/memory rm` 은
  다음 호출부터 `RULES` 에서 빠지지만 이미 그 규칙을 따른 답변이 문맥에 남아 관성으로 이어진다 —
  버그로 오해하기 쉬운 지점이라 `/new` 로 끊는 법과 함께 적었다.

## [0.4.10] - 2026-07-30
### Fixed
- **자동 압축이 지나치게 자주 걸리던 문제.** 임계값 판정에 쓰던 `usage.cache_read_input_tokens`는
  컨텍스트 크기가 아니라 **그 턴에서 일어난 모든 API 호출의 합계**였다. 도구를 호출할 때마다
  컨텍스트를 통째로 다시 읽으므로 도구를 많이 쓴 턴일수록 실제 대화 길이와 무관하게 값이 부풀었다.
  측정해 보니 파일 5개를 읽은 6턴 작업에서 실제 컨텍스트는 29k인데 합계는 **162k**로 잡혀,
  기본 임계값 100k를 그 한 턴만으로 넘겼다 — 짧은 대화에서도 압축이 계속 돌던 원인이다.
  이제 그 턴 **마지막 호출**의 입력 토큰(`usage.iterations` 끝 항목)으로 컨텍스트 크기를 추정한다.
  해당 필드가 없는 CLI 버전에서는 턴 수로 나눠 근사하며, 과소평가 쪽이라 덜 압축하는 방향으로 틀린다.
- **인라인 키보드 중복 클릭.** 버튼 제거(`editMessageReplyMarkup`)는 텔레그램 왕복이라 그 사이에
  다른 버튼을 또 누를 수 있었다. 압축처럼 오래 걸리는 동작에서 **지금 압축**과 **나중에**가 모두
  처리돼 `🗜️ 압축했습니다`와 `나중에 묻겠습니다`가 나란히 뜨는 일이 있었다. 이제 한 키보드당
  한 번만 처리한다 — `/plan` 승인 중복 실행에도 같이 적용된다.

### Added
- **압축 전에 확인받는다.** 임계값을 넘으면 바로 압축하지 않고 현재 컨텍스트 크기와 함께
  **🗜️ 지금 압축 / 나중에 / 끄기** 버튼을 보낸다. 압축은 대화를 요약본으로 바꿔 세부 내용을
  버리는 동작이라, 예고 없이 돌면 진행 중인 작업 맥락이 갑자기 사라진다.
  **나중에**를 누르면 컨텍스트가 25% 더 커지기 전까지 다시 묻지 않는다(그냥 한 번 넘기기만 하면
  다음 턴에 또 물어 결국 같은 성가심이 된다). 미뤄둔 기준은 `/new`나 `/autocompact` 재설정 시 초기화된다.
- config `autoCompactConfirm` (기본값 `true`). `false`로 두면 묻지 않고 예전처럼 바로 압축한다.

### Changed
- 자동 압축 실패가 조용히 묻히지 않고 `compact 실패: …`로 보고된다.
- **압축이 busy 락 안에서 돈다.** `/compact`와 압축 버튼이 락 없이 실행돼, 압축이 도는 2분 사이에
  보낸 메시지가 큐에 들어가지 않고 같은 세션에 동시 투입됐다 — 그 답은 뒤이어 도착한 요약본에
  덮여 통째로 사라졌다(실제 발생). 이제 다른 프롬프트와 동일하게 락을 잡고 타이핑 표시를 유지하며,
  압축 중에 들어온 메시지는 큐에 쌓였다가 끝난 뒤 처리된다.
- **누르면 즉시 `🗜️ 압축 중…`으로 응답한다.** 압축은 1~2분 걸리는데 그동안 버튼만 사라지고
  아무 반응이 없어, 버튼이 안 먹은 것으로 보였다.
- **압축 직후 다시 묻지 않는다.** 압축해도 임계값 아래로 안 내려가면 바로 다음 턴에 또 물었다.
  이제 압축 후 컨텍스트를 기준으로 유예 기준을 다시 잡는다.

## [0.4.9] - 2026-07-30
### Added
- **혼잣말 접두사 `//` — 봇이 무시하는 메시지.** 작업이 도는 중에 딴 주제의 아이디어가 떠올라
  바로 보내면 대기큐에 들어가 작업 후 프롬프트로 실행돼버리고, 세션 콘텍스트가 꼬일 위험이 있었다.
  이제 메시지를 `//`로 시작하면 봇이 완전히 무시하고 👀 리액션만 남긴다 — 채팅에 메모는 그대로
  남으니 보관·다시 꺼내기는 텔레그램이 해주는 셈이다. 사진 캡션 `//…`도 동일하게 무시하며,
  레이트리밋·busy·로컬 락 등 어떤 상태에서도 큐에 들어가지 않는다. (기존에는 `//메모`가 명령어로도
  안 걸리고 프로바이더에 그대로 전달됐다.)

### Changed
- **`/provider`·`/model`을 버튼으로 전환.** `/autocompact`와 같은 방식으로, 인자 없이 부르면 현재 값을
  ✅로 표시한 인라인 버튼이 함께 온다. `/provider`는 `claude` · `codex` · config 기본값,
  `/model`은 Claude일 때 `fable` · `opus` · `sonnet` · `haiku` + 기본값(Codex는 제안할 별칭이 없어
  기본값 버튼만). 휴대폰에서 `/provider codex`를 타이핑할 필요가 없다. 명령어 인자 방식도 그대로
  동작하며, 버튼과 명령어가 `handleProvider()`·`handleModel()` 하나씩을 공유하므로 동작이 갈라지지
  않는다. `/model` 버튼에는 표시 시점의 provider를 실어 보내, 누르기 전에 provider를 바꿔도 엉뚱한
  쪽 모델이 바뀌지 않는다.

## [0.4.8] - 2026-07-30
### Added
- **`/local` — 로컬 `ctb` 세션을 텔레그램에서 종료.** 데스크탑에 `ctb`를 켜둔 채 나오면 머신 전체 락이
  걸려 봇이 모든 메시지에 "로컬 세션이 활성화되어 있습니다"로만 답하고, 집에 갈 때까지 아무것도 못 하는
  상태가 됐다. 이제 `/local`이 락을 쥔 PID와 실행 경과 시간을 보여주고 종료 버튼을 함께 보낸다.
  그 안내 메시지 자체와, 봇 작업이 없을 때의 `/stop`에도 같은 버튼이 붙는다(`/local kill`은 버튼 생략).
  종료는 PID 단건이 아니라 **프로세스 그룹**에 `SIGTERM`을 보낸다 — 터미널의 `Ctrl-C`와 같은 경로라
  `ctb`가 자식 claude까지 정리하고 락 해제·세션 요약 알림을 정상 수행한다. 그룹 전송이 안 되는
  환경이면 PID로 폴백하고, 5초 안에 락이 풀리지 않으면 `SIGKILL`한다.

## [0.4.7] - 2026-07-30
### Changed
- **`/autocompact` 임계값을 버튼과 축약 표기로 설정.** 휴대폰에서 `/autocompact 120000`처럼 0을 여러 개
  치는 게 번거로웠다. 이제 인자 없이 `/autocompact`를 보내면 현재값과 함께 프리셋 인라인 버튼
  (50k / 100k / 150k / 200k / 끄기 / 기본값)이 나오고, 직접 입력할 때도 `120k` · `80,000`
  같은 축약·구분자 표기를 받는다(기존 `120000` 형식도 그대로). 표시 문구도 `100000 토큰` → `100k 토큰`으로
  읽기 쉽게 바꿨다. 명령어 처리와 버튼 콜백이 같은 경로(`handleAutoCompact`)를 타므로 동작은 동일하다.
  10k~1m 범위를 벗어난 값은 거절한다 — `m` 접미사를 받게 되면서 `100m` 같은 오타 한 번에 자동 압축이
  조용히 영구 비활성화될 수 있었고(실제로 밟았다), 반대로 너무 작으면 매 턴 압축이 돌아버린다.

## [0.4.6] - 2026-07-24
### Added
- **봇이 이미지를 텔레그램으로 전송.** 에이전트가 답변 끝에 `[[ctb-image: 파일명 | 캡션]]` 마커를 붙이면
  bot.mjs가 마커를 떼고 그 파일을 사진으로 보낸다. 파일은 `projectDir/.ctb-outbox/`에서만 읽는다 —
  마커의 값에서 basename만 취해 경로 탈출을 원천 차단하고, 실제 그 폴더 안의 파일이며 허용 확장자
  (`png/jpg/jpeg/gif/webp`)·10MB 이하여야 하며, 폴더 밖을 가리키는 심볼릭 링크도 거부한다. 아웃박스를
  `projectDir` 아래 둔 건 Claude와 Codex(`workspace-write` 샌드박스) 둘 다 쓰기 가능한 위치라서다.
  전송법은 시스템 프롬프트로 provider에 자동 안내되며, `"sendImages": false`로 끌 수 있다. 나가는
  경로는 텍스트 전용이던 것을 Node 18+ 내장 `FormData`/`Blob` 기반 `sendPhoto`로 확장했다(의존성 0 유지).
### Fixed
- **provider 우선순위 문서가 실제 동작과 달랐던 문제.** 0.4.1에서 `ctb.mjs`가 `/provider` override
  (`state.provider`)를 읽도록 바뀌었는데 문서는 갱신되지 않아, README(영/국문)는 "로컬 `ctb`는 계속
  `config.provider`를 쓴다"고, `ctb --help`는 `--provider flag → config.provider → claude`라고
  안내하고 있었다. 실제 우선순위인 `--provider` 옵션 → state override → `config.provider` → `claude`로
  네 곳 모두 수정했다. 동작 변경은 없다(help 출력 문구만 바뀜).

## [0.4.5] - 2026-07-20
### Fixed
- **로컬 `ctb`가 텔레그램 세션을 이어받지 못하던 버그(0.4.3 회귀).** 0.4.3에서 세션을 방별로 옮기며
  `state.sessionId` → `state.sessions[chatId].sessionId` 구조가 됐고 마이그레이션이 최상위 키를
  지우는데, `ctb.mjs`는 그대로 최상위에서 읽고 있었다. 결과적으로 세션 ID가 항상 `undefined`라
  `--resume` 없이 매번 새 세션으로 떴다 — Claude·Codex 둘 다 해당. 이제 방별 구조를 읽고,
  0.4.3 이전 state를 위해 최상위 키 폴백도 유지한다.
### Added
- **`ctb --chat <id>`** — 이어받을 방을 지정한다. 지정하지 않으면 `allowedChatId`의 첫 번째 방
  (보통 소유자 DM)을 쓴다 — bot.mjs의 구버전 마이그레이션이 주 방을 고르는 규칙과 같다.
  DM 말고 그룹 세션을 로컬에서 이어받을 때 쓴다.

## [0.4.4] - 2026-07-20
### Fixed
- **그룹에서 `/명령@봇이름`이 명령어로 인식되지 않던 버그.** 그룹 채팅에서 텔레그램 자동완성으로
  명령어를 고르면 `/provider`가 아니라 `/provider@MyBot` 형태로 전송되는데, 명령어 파싱이 정확 매칭
  (`text === "/provider"`)이라 어디에도 걸리지 않고 일반 프롬프트로 에이전트에 넘어갔다 — 명령이 실행되는
  대신 LLM이 그 문장에 대해 아무 말이나 지어냈다. `/provider`뿐 아니라 모든 명령어가 같은 문제를 겪었음.
  이제 `handle()` 진입부에서 첫 토큰의 `@봇이름`만 벗겨낸다. 시작 시 `getMe`로 자기 username을 받아
  대소문자 무시 비교하므로, 같은 그룹에 있는 다른 봇을 향한 `/명령@다른봇`은 건드리지 않는다.

## [0.4.3] - 2026-07-16
### Added
- **방별 세션 분리** — DM과 각 그룹이 서로 독립된 Claude·Codex 세션을 가진다
  (`state.sessionId` → `state.sessions[chatId]`). 한 config에 서로 무관한 방을 여러 개 등록해도
  한 방의 대화가 다른 방 맥락으로 넘어가지 않는다. 접근은 `chatBucket`/`getSid`/`setSid` 접근자로
  일원화했고, 구버전 state의 전역 `sessionId`는 첫 실행 때 주 방(`allowedIds[0]`)으로 자동
  이관된다. 그룹 안에서는 참여자 전원이 그 그룹의 단일 세션을 공유한다(사람별 분리는 범위 밖).
- **방별 동시 실행** — 방끼리 병렬로 처리한다. DM에서 긴 작업이 돌아도 그룹은 기다리지 않는다.
  실행 상태(`busy`/`child`/`typing`/`prevSession`/`stopping`/`queue`)를 전역 변수에서 방별
  `chatRuntime` 맵으로 분리했다. 한 방 안에서는 기존대로 직렬 처리 + 큐 병합이고, 병합은 항상 한 방
  안에서만 일어나므로 세션이 섞이지 않는다. 예약 작업은 전용 슬롯(`__cron__`)에서 돌아 예약끼리는
  직렬, 사용자 방과는 병렬이다. 레이트리밋(계정 단위)과 로컬 `ctb` 락(머신 단위)은 성격상 전역으로
  유지했다.
- **`/restart` 중단 알림** — 재시작은 프로세스를 통째로 내리므로 방별 격리와 무관하게 다른 방 작업까지
  같이 죽는다. 실행 중이거나 큐가 쌓인 방에 중단 사실을 알린다(놀고 있는 방은 소음이라 제외).
### Changed
- `/stop`이 **그 방의** 프로세스와 큐만 중단한다 — 다른 방의 작업은 영향받지 않는다.
- `/reserve` 상태 표시가 모든 방의 대기 메시지를 합산해 보여준다.

## [0.4.2] - 2026-07-14
### Added
- 그룹 채팅에서 메시지를 보낸 사람을 `[From: 이름 (@username)]` 형태로 프롬프트에 표시한다(1:1 채팅은
  한 명뿐이라 표시하지 않음). `busy` 중 쌓인 메시지를 한 번에 합쳐 보내는 큐 병합 시에도 줄마다 발신자를
  구분해 표기한다 — 이전엔 마지막 발신자만 남아 앞서 온 메시지들의 발신자 정보가 유실됐었다.
### Fixed
- `ctb`가 provider를 고를 때 `state.json`의 `provider`(텔레그램 `/provider`로 전환한 값)를 무시하고
  `config.provider`만 보던 버그. 텔레그램에서 `/provider codex`로 전환해둔 상태에서 `ctb`를 실행하면
  엉뚱하게 Claude 세션을 resume해 로컬 대화가 텔레그램 쪽 세션과 분리되던 문제였음 — 우선순위를
  `--provider` → `state.provider` → `config.provider` → `claude`로 수정해 bot.mjs의 `currentProvider()`와
  동일하게 맞췄다.
- `ctb` 세션 종료 알림(`notifyTelegram`)이 `allowedChatId`가 배열일 때 `chat_id`에 배열을 그대로 넣어
  텔레그램 API가 `400 Bad Request`로 거부하던 버그 — 각 chat_id를 순회하며 개별 전송하도록 수정.
- 레이트리밋으로 메시지가 reserve 큐에 쌓인 상태에서 `/provider`로 다른 provider로 전환해도 큐가 그대로
  묶여 있던 버그 — 전환 시 예약을 즉시 풀고 새 provider로 큐를 이어서 처리하도록 수정.

## [0.4.1] - 2026-07-10
### Changed
- `ctb`의 provider 결정 우선순위(`--provider` → `config.provider` → `claude`)를 도움말에 명시했다.
- `/model` 상태·설정 안내를 provider별로 분리했다. Claude에서는 모델 별칭을 제안하고, Codex에서는
  Claude 별칭을 노출하지 않고 전체 Codex 모델 ID와 `codexModel`/CLI 기본값 사용법을 안내한다.

## [0.4.0] - 2026-07-10
### Added
- `config.json`의 `provider`로 Claude 또는 Codex를 텔레그램 봇의 메인 실행자로 선택할 수 있다.
- `ctb <config> --provider codex`가 `state.codexSessionId`를 읽어 대화형 `codex resume` 세션을 실행한다.
- `/provider`로 현재 Telegram provider를 확인하고 `/provider claude`, `/provider codex`로 state override를
  저장하거나 `/provider default`로 config 기본값을 복원할 수 있다. config 파일은 수정하지 않으며,
  Claude와 Codex의 세션·모델 override는 각각 보존된다.
- README에 마지막으로 함께 검증한 Claude Code, Codex CLI, Ollama 버전과 호환성 확인 절차를 기록했다.
- `/status`에서 봇 호스트에 실제 설치된 세 CLI 버전을 함께 표시한다.
- 기존 Codex 폴백 재설계 — Claude 레이트 리밋·크레딧 부족 시 `"codexFallback": true`이면 reserve 큐로 넘기기 전에
  `codex exec`를 실행한다. Codex 세션은 `state.codexSessionId`에 별도로 저장하고 이후
  `codex exec resume <세션ID>`로 이어간다.
- Claude와 Codex 세션은 서로 호환되지 않으므로, 성공한 Codex 폴백 내용을
  `.claude-bot/codex-handoff.md`에 누적하고 이후 Claude 호출의 시스템 프롬프트에 최근 handoff를 주입한다.
### Changed
- minor 버전을 `0.4.0`으로 올렸다.
- 한도 초과 처리 순서 변경: Codex 폴백 성공 시 `/reserve` 자동 재시도를 예약하지 않고, Codex가 꺼져 있거나
  실패했을 때만 기존 Ollama 폴백/ reserve 흐름으로 내려간다.
- 일반 메시지, 첨부 파일, 예약 작업과 cron 자연어 해석이 선택된 provider를 사용한다. `/plan`, `/compact`,
  자동 compact는 Claude provider에서만 동작한다.
- `/status`가 활성 provider에 맞는 모델·세션·fallback 상태를 표시하고 세 CLI 버전을 함께 확인한다.
- Codex fallback 응답 머리말을 짧은 `Codex fallback`/`Codex 폴백` 표시로 줄였다.
- 로컬 lock 안내에서 특정 provider를 뜻하는 `ctb claude` 표현을 중립적인 `ctb`로 변경했다.
- 기본 설정 예시의 Claude 권한을 `bypassPermissions`에서 `acceptEdits`로 낮췄다.

## [0.3.41] - 2026-07-09
### Changed
- Ollama 폴백이 이제 **대화 맥락을 이어받는다** — 기존에는 `localhost:11434/api/chat` HTTP API를
  직접 호출해 직전 프롬프트 1개만 단발로 처리했고, 헤더에도 "세션은 이어지지 않아요"라고 안내했음.
  이제 `ollama launch claude --model <m> --yes -- … --resume <세션ID>` 로 **Claude Code CLI 자체를
  로컬 모델로 구동**해, 폴백·`/ollama` 모드 응답이 Claude와 나누던 대화 스레드를 이어감
  - 반환된 `session_id`를 `state.sessionId`에 저장해 다음 턴까지 체이닝
  - 기본 모델 `phi3:mini` → `qwen3.5:4b` (실제 세션 resume 테스트로 검증된 모델)
  - config 옵션 추가: `ollamaBin`(실행 파일 경로), `ollamaTimeout`(응답 대기 ms, 기본 360000 —
    로컬 4B 모델 콜드스타트가 첫 응답까지 수 분 걸릴 수 있어 넉넉히 설정)
  - ⚠️ 완전한 세션 승계가 아니라 **최선 노력(best-effort)**임을 문서에 명시. 로컬 모델의 런타임
    컨텍스트 창(`num_ctx`)은 Ollama 기본값이 ~4K라 긴 Claude 세션(관측상 ~15K)은 잘려 들어감.
    `Modelfile`로 `num_ctx`를 키운 변형 모델을 만들어 `ollamaModel`로 지정하면 완화되지만, KV 캐시가
    RAM을 잡아먹으므로 크기는 머신에 맞춰야 함 — 8GB 머신에선 ~6K가 안전 상한(32K는 스왑으로 먹통).
    README(영/국문)의 `ollamaModel` 항목과 Ollama 폴백 설명에 관련 안내·한계 추가
  - 폴백 포지셔닝 정직화: 작은 로컬 모델은 코딩·도구 호출을 Claude처럼 못 하므로
    "완전한 코딩 대체재"가 아니라 **경량 텍스트 조수**로 명시. 잘하는 역할(Claude 복귀 후
    시킬 요청을 미리 정리·초안 작성, 요약, 메모 정리)을 안내로 추가
### Fixed
- launchd로 뜬 봇에서 Ollama 폴백이 `spawn ollama ENOENT`로 실패하던 문제 — launchd 데몬은
  로그인 셸을 거치지 않아 `.zshrc`/`brew shellenv`가 설정한 PATH(`/opt/homebrew/bin` 등)를
  상속받지 못함(`claudeBin`과 동일 원인). `ollamaBin` 미지정 시 흔한 Homebrew/시스템 설치
  경로를 자동으로 탐색하도록 수정

## [0.3.40] - 2026-07-06
### Fixed
- `/status`가 npm에 아직 배포되지 않은 로컬 버전을 npm 레지스트리의 구버전과 비교해
  실제로는 최신인데도 "업데이트 있음"처럼 `버전: 0.3.39 → 0.3.36 ✨`로 잘못 표시하던 버그
  - 버전 문자열이 다르기만 하면 화살표를 붙였고, 어느 쪽이 더 최신인지는 비교하지 않았음
  - 이미 있던 `isNewer()` semver 비교 함수를 사용하도록 수정 — 진짜로 더 최신 버전이 있을 때만 화살표 표시

## [0.3.39] - 2026-07-06
### Added
- `/autocompact` 명령어 — 자동 컴팩션 토큰 임계값(`autoCompactThreshold`)을 런타임에 확인·변경
  - `/autocompact` — 현재 임계값 확인
  - `/autocompact <숫자>` — 임계값 변경 (state에 저장, 재시작 후에도 유지)
  - `/autocompact off` — 자동 컴팩션 비활성화 (`0`으로 설정)
  - `/autocompact default` — config.json의 값(또는 기본값 100000)으로 초기화
  - 기존에는 config.json을 직접 수정하고 봇을 재시작해야만 값을 바꿀 수 있었음

## [0.3.38] - 2026-07-04
### Fixed
- 봇이 busy 상태일 때 사진(첨부)이 포함된 메시지가 연달아 들어오면, 대기열을 합치는 과정에서
  마지막 메시지를 제외한 앞선 메시지들의 사진이 통째로 사라지던 버그
  - `drainQueue()`가 텍스트/캡션만 번호 붙여 병합하고 `{...group[group.length-1].msg, ...}`로
    마지막 메시지 필드만 남겨, 먼저 온 사진의 `photo`/`_mediaGroup`이 유실됨
  - 큐에 쌓인 모든 메시지의 첨부(사진/문서/음성/영상)를 순서대로 모아 `_mediaGroup`으로
    합치도록 수정 — Claude가 먼저 온 사진도 모두 확인 가능

## [0.3.37] - 2026-07-04
### Fixed
- `busy` 플래그 교착(deadlock) 버그 — `/cron add`, `/plan` 승인 실행, 일반 메시지 처리 3곳에서
  `busy = true` 직후의 `await tg("sendChatAction", ...)` 호출이 `try/catch` 밖에 있어,
  일시적 네트워크 오류로 이 호출이 실패하면 `busy`가 영영 `true`로 남아버리는 문제
  - `/stop`은 `currentChild`가 없어 "실행 중인 작업 없음"이라 답하지만, 새 메시지는 전부
    무한 대기열에만 쌓이는 모순된 멈춤 상태가 발생
  - `sendChatAction` 호출을 각 `try` 블록 안으로 이동해, 실패해도 기존 `catch`/`finally`가
    `busy`/`currentTyping`을 정상적으로 초기화하도록 수정

## [0.3.36] - 2026-07-03
### Fixed
- `/plan`(0.3.35)에서 `send()`가 제너레이터 `chunks()`를 배열처럼 `.length`로 인덱싱해
  모든 메시지가 무응답으로 사라지던 버그
  - `chunks()`는 `function*`이라 `.length`가 `undefined` → 루프 조건이 항상 false로 평가되어
    본문이 한 번도 실행되지 않음 (예외도 로그도 없이 조용히 실패)
  - `const cs = chunks(text)` → `const cs = [...chunks(text)]`로 배열 변환하여 수정
  - 자세한 원인 분석: `docs/incidents/2026-07-03-plan-send-silent-failure.md`

## [0.3.35] - 2026-06-27
### Added
- `/plan <요청>` — Plan Mode
  - 봇의 `permissionMode` 설정과 무관하게 강제로 `plan`(읽기 전용) 모드로 1회 실행
  - 계획 내용과 함께 텔레그램 인라인 버튼(✅ 진행 / ❌ 취소) 전송
  - **진행** 클릭 시 같은 세션을 봇의 실제 `permissionMode`로 이어서 계획 실행
  - **취소** 클릭 시 세션 변경 없음 (애초에 plan 모드라 편집 자체가 없었음)
  - `/new`로 세션이 바뀌면 대기 중인 승인은 자동 만료
  - 헤드리스 `--print` 모드는 진짜 `ExitPlanMode` 툴 인터셉트(중간 정지)가 불가능해, plan 실행 → 승인 → resume 실행의 2단계 방식으로 구현

## [0.3.34] - 2026-06-27
### Added
- 커스텀 명령어 — `config.json`의 `commands` 필드로 프로젝트별 `/명령어` 정의
  - `run`: 쉘 명령어 실행, 출력을 텔레그램으로 전송
  - `/cmd arg1 arg2` 형태로 인자 전달 가능
  - Telegram `/` 자동완성 메뉴에 자동 등록
  - Claude 와 독립 실행 (busy 상태와 무관)
  - 60초 타임아웃, 4000자 초과 시 truncate

```json
"commands": {
  "deploy": { "run": "npm run deploy", "description": "프로젝트 배포" },
  "logs":   { "run": "tail -n 50 ./app.log", "description": "최근 로그" }
}
```

## [0.3.33] - 2026-06-27
### Fixed
- 0.3.31 에서 `-p`를 args 맨 앞에 두어 `--output-format`이 `-p`의 값으로 먹히는 버그
  - 모든 `--flag value` 옵션 뒤에 `-p -- <prompt>` 배치로 수정 (터미널 테스트 검증 순서)

## [0.3.32] - 2026-06-27
### Added
- 자동 컴팩션 — `cache_read_input_tokens` 가 임계값 초과 시 `/compact` 자동 실행
  - 기본값 100,000 토큰 (`config.json` 에 `"autoCompactThreshold": 100000` 으로 조정 가능)
  - 0 으로 설정하면 비활성화
  - 압축 후 텔레그램으로 알림 전송

## [0.3.31] - 2026-06-26
### Fixed
- `-`로 시작하는 메시지가 여전히 옵션으로 오해되던 문제 (0.3.28~0.3.30 미해결)
  - `-p<prompt>` 인라인 묶기는 claude CLI 파서가 미지원 (`-p` + `-prompt` 로 분리)
  - 프롬프트를 모든 옵션 뒤 `--` 구분자 다음에 위치 인자로 전달하도록 변경

## [0.3.30] - 2026-06-26
### Fixed
- `-p <prompt>` → `-p<prompt>` (붙여쓰기) 방식으로 변경 (실패 — 0.3.31에서 재수정)

## [0.3.29] - 2026-06-26
### Fixed
- 구버전 `claude` CLI에서 `--print` 미지원으로 발생하는 "unknown option" 에러
  - 시작 시 `claude --help` 로 지원 여부 탐지 → 구버전이면 `-p <prompt>` 자동 폴백

## [0.3.28] - 2026-06-25
### Fixed
- `-`로 시작하는 메시지(예: `-9038502는 잔액이고…`)가 Claude CLI 옵션으로 오해되는 문제
  - `-p <prompt>` → `--print=<prompt>` 방식으로 변경

## [0.3.26] - 2026-06-24
### Fixed
- `"session limit"` 에러 미감지 문제 — `isFallbackError` / `classifyClaudeError` 에 추가
- `"resets 7:20pm"` 형식 파싱 실패 — `parseResetTime` regex에서 `at` 없이도 시간 추출
### Changed
- 레이트리밋 에러 시 메시지를 큐에 보관 후 리셋 시간에 자동 재시도 (busy 큐와 동일 메커니즘)
  - 제한 중 추가로 보내는 메시지도 큐에 쌓임
  - `/reserve` → 대기 현황 보기, `/reserve rm` → 큐 전체 취소

## [0.3.25] - 2026-06-23
### Added
- `/ollama` 명령어 — Ollama 채팅 모드 수동 토글
  - 켜는 동안 메시지는 Claude 없이 Ollama로 직접 처리 (Claude 세션은 유지)
  - 다시 `/ollama` 로 끄면 Claude 모드로 복귀
### Changed
- Ollama 요청을 `/api/generate` → `/api/chat` 으로 전환
  - 시스템 프롬프트 지원: `cfg.persona` + 간결체 instruction 주입
  - 응답 품질 개선 (특히 작은 모델에서 효과적)

## [0.3.24] - 2026-06-23
### Fixed
- ctb 종료 시 요약이 항상 SKIP으로 반환되는 문제 수정
  - 세션 컴팩션 후 `---ctb:start---` 마커가 묻히는 문제 → 최근 대화 기반 요약으로 변경

## [0.3.23] - 2026-06-23
### Added
- 텔레그램 메시지 메타데이터 프롬프트 주입
  - 포워드 메시지: 출처(유저/채널/숨김) 자동 감지 → `[Forwarded from: ...]` 표기
  - 리플라이: 원본 메시지 내용(최대 300자) → `[Replying to ...: "..."]` 표기

## [0.3.22] - 2026-06-23
### Changed
- Codex 폴백 → Ollama 폴백으로 교체 (fetch 기반, 의존성 0 유지)
  - config.json에 `"ollamaFallback": true`, `"ollamaModel": "phi3:mini"` 로 설정
  - Claude 레이트리밋·크레딧 부족 시 로컬 Ollama 모델로 자동 대체

## [0.3.21] - 2026-06-23
### Added
- `/testfallback` 명령어 — Codex 폴백 연결을 직접 테스트

## [0.3.20] - 2026-06-23
### Added
- Codex 폴백 — Claude 레이트리밋·크레딧 부족 시 OpenAI Codex로 자동 대체 응답
  - config.json에 `"codexFallback": true` 로 활성화, `codex` CLI가 설치된 경우에만 동작
  - 응답 앞에 "🌙 Claude가 잠시 쉬고 있어요" 안내 문구 자동 삽입

## [0.3.19] - 2026-06-19
### Added
- `/compact` 명령어 — 세션을 유지한 채 컨텍스트를 요약 압축 (`/new` 없이 공간 확보)
- "Prompt is too long" 에러 감지 → `/compact` 또는 `/new` 안내 메시지로 대체

## [0.3.18] - 2026-06-19
### Fixed
- 같은 이미지를 여러 번 보내면 파일이 덮어써지는 버그 — 파일명에 타임스탬프 추가 (`tg-<ts>-<id>.<ext>`)

## [0.3.17] - 2026-06-18
### Fixed
- `/remember` 메모리가 persona에 덮어쓰여 무시되는 문제 — 메모리를 시스템 프롬프트 맨 앞에 배치하고 헤더를 `## RULES (must follow before anything else)` 로 강화

## [0.3.16] - 2026-06-18
### Changed
- `allowedChatId` 를 배열로도 지정 가능 — `["chatId1", "chatId2"]` 형태로 여러 사용자 허용 (기존 문자열 형태 하위 호환)
- cron 결과·업데이트 알림을 모든 허용 채팅에 브로드캐스트

## [0.3.15] - 2026-06-17
### Added
- `/reserve` 명령어 — 사용 한도 초과 시 리셋 시점에 재시도 예약, `/reserve <다른 메시지>` 로 내용 변경, `/reserve rm` 으로 취소

## [0.3.14] - 2026-06-17
### Added
- 여러 이미지 동시 전송 지원 — `media_group_id` 감지 후 1초 대기, 그룹 내 모든 이미지를 한 번에 Claude에 전달

## [0.3.13] - 2026-06-16
### Added
- `ctb` 세션 시작 시 `---ctb:start---` 마커 삽입 — 종료 요약이 텔레그램 이전 대화를 제외하고 터미널 세션 작업만 요약

## [0.3.12] - 2026-06-16
### Fixed
- `ctb` 종료 알림 fetch 실패 — `dns.setDefaultResultOrder("ipv4first")` 추가로 IPv6 우선 시도 문제 해결
- `ctb` 알림 언어 — `cfg.lang` 미설정 시 `$LANG` 환경변수로 폴백
- `ctb` 알림 메시지에 `[터미널]` / `[local]` 레이블 추가로 텔레그램 채팅 응답과 구분

## [0.3.11] - 2026-06-15
### Fixed
- `ctb` 종료 요약을 config `lang` 에 맞는 언어로 전송 (ko 설정 시 한국어)

## [0.3.10] - 2026-06-15
### Fixed
- `ctb` Ctrl+C 종료 시에도 텔레그램 요약 알림 전송
- 세션 종료 요약을 10단어 이내 짧은 구문으로 제한

## [0.3.9] - 2026-06-15
### Added
- 업데이트 자동 감지 — 시작 시 24h 주기로 npm 최신 버전 확인, 새 버전이 있으면 버전당 1회 텔레그램 알림

## [0.3.8] - 2026-06-15
### Added
- `ctb` 세션 종료 시 텔레그램으로 한 줄 요약 자동 전송 — config 에 `"ctbNotify": false` 로 비활성화 가능

## [0.3.7] - 2026-06-15
### Fixed
- Claude 에러 분류를 JSON 경로(`is_error: true`)에도 적용 — 0.3.6 에서 비정상 종료만 처리하던 것을 JSON 응답 에러까지 확장

## [0.3.6] - 2026-06-14
### Fixed
- Claude 에러 메시지 분류 — 크레딧 부족·레이트리밋·서버 과부하·컨텍스트 초과를 감지해 날 것 에러 대신 안내 메시지 표시

## [0.3.5] - 2026-06-14
### Added
- `/status` 에 npm 최신 버전 표시 — 업데이트가 있으면 `0.3.4 → 0.3.5 ✨` 형식으로 알림

## [0.3.4] - 2026-06-14
### Fixed
- `/stop` 이 작업 중에 무시되는 버그 수정 — 폴링 루프의 `await handle()` 로 인해 `/stop` 메시지가 현재 작업 완료까지 처리되지 않던 문제

## [0.3.3] - 2026-06-13
### Fixed
- `ctb` 전역 설치 시 세션 연결 안 되는 문제 — config 탐색 순서를 `cwd` 우선으로 변경

## [0.3.2] - 2026-06-13
### Fixed
- 한국어 `/help` 텍스트에서 `/cron` 줄 중복 제거

## [0.3.1] - 2026-06-13
### Added
- 퍼시스턴트 메모리 — `/remember <내용>` 으로 저장, `/new` 후에도 유지, 매 대화에 자동 주입
- `/memory` — 메모리 확인 · `/memory clear` 로 삭제
- `init` 기본 파일명을 `mybot.json` 으로 변경

## [0.3.0] - 2026-06-12
### Added
- `/stop` — 진행 중인 작업 중단 · `--reset` 옵션으로 세션 롤백
- 메시지 큐 — busy 중 수신된 메시지를 대기 후 자동 처리, 여러 개면 시간 정보와 함께 일괄 병합
- `init` 파일명 직접 지정 가능 (`ctb init mybot.json`)

## [0.2.7] - 2026-06-11
### Added
- `fable` 모델을 MODEL_SUGGESTIONS 및 모델 권유 프롬프트에 추가

## [0.2.6] - 2026-06-11
### Added
- 현재 모델을 시스템 프롬프트에 주입 — Claude가 질문 난이도에 따라 상위 모델 전환 스스로 권유

## [0.2.5] - 2026-06-10
### Added
- `/model` — 런타임 모델 보기·전환 (`/model sonnet`, `/model default` 등)

## [0.2.4] - 2026-06-09
### Changed
- 데이터(state·attachments)를 `.claude-bot/` 숨김 폴더로 이동, 기존 데이터 자동 이주

## [0.2.3] - 2026-06-08
### Added
- `/status` — 봇 이름·버전·모델·세션·예약 작업·프로젝트 경로 표시

## [0.2.1] - 2026-06-07
### Added
- i18n — 영어 기본 + 한국어, `from.language_code` 로 사용자별 자동 판별
- `/` 명령 자동완성 메뉴 등록 (`setMyCommands`)
- 한 줄 인라인 코드를 `<pre>` 블록으로 렌더링 (텔레그램 복사 버튼)

## [0.2.0] - 2026-06-06
### Added
- `ctb` CLI — 로컬 터미널에서 텔레그램 봇 세션 공유
- `/cron` — 예약 작업 자연어 추가·삭제·목록
- `/restart` — 문법 검사 후 안전 재시작
- 조용한 예약 작업 — 출력이 비었거나 `SKIP` 이면 전송 안 함

## [0.1.0] - 2026-06-01
### Added
- 최초 릴리스 — Telegram → `claude -p` 헤드리스 브리지, zero-dependency, 단일 파일
- 파일 첨부 지원, HTML 포맷팅, IPv4 우선 설정
- `launchd` 데몬 예시 (`com.claudebot.example.plist`)
