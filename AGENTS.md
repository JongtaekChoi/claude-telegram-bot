# Agent Instructions

이 레포의 작업 규칙은 [CLAUDE.md](CLAUDE.md) 에 있다. **먼저 읽는다.** Codex 는 `CLAUDE.md` 를
자동으로 읽지 않아서, 클로드 한도로 Codex 에 넘어간 순간 규칙을 모르는 상태가 되기 때문에 이
파일을 둔다. 규칙을 두 벌로 적으면 갈라지므로 여기에는 가장 자주 놓치는 두 가지만 다시 적는다.

- **작업 대기열은 [docs/BACKLOG.md](docs/BACKLOG.md) 하나다.** "다음에 뭘 할까"는 거기서 찾고,
  끝낸 항목은 지우고, 커밋했지만 실봇 확인이 남았으면 "검증 빚"으로 옮긴다.
- **기능을 고치기 전에 `docs/design/` 의 해당 설계 문서를 읽는다.** 구현이 기존 결정을 뒤집었으면
  코드와 같은 커밋에서 문서도 고친다.

`git push` · `npm publish` 전에는 반드시 사용자에게 먼저 확인한다.
